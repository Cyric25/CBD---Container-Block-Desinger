# Datei-Map: Plugin „Container Block Designer"

_Stand: 2026-08-30 · Plugin-Version 3.1.117_

Navigationshilfe auf Dateiebene. Die fachlichen Details stehen in `CLAUDE.md`
— dort insbesondere die Abschnitte zum Content-Importer, Block-Serializer,
Seitenimport, zur Icon-Bibliothek und zur Icon-Größe.

Diese Datei entstand am 2026-08-10 im Zuge des Vorhabens „Seitenimport"
(`docs/archiv/PLAN-Seitenimport.md`, AP-4.1). Sie war die letzte fehlende
Datei-Map des Projekts; Theme und „Eigene WP Blocks" hatten längst eine.

## Wurzel und Bootstrap

| Datei | Zweck | Wichtige Funktionen/Inhalte | Hängt ab von |
|---|---|---|---|
| `container-block-designer.php` | Hauptdatei, Singleton `ContainerBlockDesigner` | Konstanten (`CBD_VERSION`, `CBD_PLUGIN_DIR`, `CBD_TABLE_BLOCKS`, seit Vorhaben „Fragenwand" AP-2.1 zusätzlich `CBD_TABLE_NOTES`), `load_dependencies()` mit allen `require_once`, `init()` auf `init` Priorität 0, `create_block_editor_role()`. **Seit „Klassenmodus-Live" AP-1.5:** `load_dependencies()` bindet `includes/class-cbd-klassenpuls.php` direkt nach `class-cbd-classroom.php` ein (reine Statik, kein Singleton — instanziiert sich nicht selbst); `init()` ruft — nach demselben Muster wie `CBD_Block_Content_API::init()` und hinter `class_exists('CBD_Klassenpuls')` — `CBD_Klassenpuls::init()` auf, das nur `register_routes()` an `rest_api_init` hängt. Ob der Taktgeber im Browser überhaupt startet, entscheidet ausschließlich `CBD_Classroom::enqueue_frontend_assets()` (siehe dort) | `includes/*` |
| `composer.json` / `composer.lock` | Abhängigkeiten (mPDF, TCPDF-Rückfall, Dev: phpunit, wpcs) | – | – |
| `create-plugin-zip.js` | Verteilungspaket bauen | erhöht die Version selbstständig, ruft vorher `tools/check-php74.php`, stellt **`composer dump-autoload --no-dev`** her und danach wieder den Dev-Autoloader. **Diesen Schritt nie entfernen** — ein Autoloader mit Dev-Paketen bindet phpunit ein und ergibt HTTP 500 auf der Zielinstallation. Schließt seit AP-1.fix3 zusätzlich `vendor/bin/` und `vendor/mpdf/mpdf/phpunit.xml` über ein eigenes `excludeExactPaths`-Array aus — **pfadgenau, nicht per Segmentname**, damit kein künftiger `bin`-Ordner außerhalb von `vendor/` versehentlich mitentfällt | `tools/check-php74.php` |
| `syntax-check.js` | `php -l` über alle Plugin-Dateien | – | – |
| `remove-debug-logs.js` | Werkzeug zum Entfernen von Debug-Ausgaben | – | – |
| `package.json` | npm-Skripte für die Bauwerkzeuge | – | – |

## Kernklassen (`includes/`)

| Datei | Zweck | Wichtige Funktionen/Inhalte | Hängt ab von |
|---|---|---|---|
| `class-service-container.php` | Einfache Abhängigkeitsverwaltung | `cbd_get_service()`; verwaltet database, style_loader, block_registration, ajax_handler, admin | – |
| `class-autoloader.php` | PSR-4-Rückfall ohne Composer | – | – |
| `class-cbd-database.php` | Datenbank-Zugriffsschicht | delegiert an den Schema-Manager | `Database/class-schema-manager.php` |
| `class-cbd-block-registration.php` | Registriert die Container-Blöcke aus der Datenbank | `register_blocks()`, `render_block()` (fängt Throwables pro Block ab), `render_icon()`, Asset-Einbindung für Editor und Frontend, Isolierung von Inline-Skripten in IIFEs | `CBD_TABLE_BLOCKS`, `class-cbd-style-loader.php` |
| `class-cbd-style-loader.php` | Erzeugt das dynamische CSS je Block-Design | Transient-Zwischenspeicher, `clear_styles_cache()` | `CBD_TABLE_BLOCKS` |
| `class-cbd-admin.php` | Admin-Oberfläche und Menü | `add_admin_menu()`, `render_database_repair_page()` (zeigt die Spalten von `cbd_blocks` **und seit 3.1.111 den Vorhanden-Status aller fünf Plugin-Tabellen**), `handle_database_repair()` — **legt `slug` an und füllt sie aus `name`; ruft seit 3.1.111 zusätzlich `CBD_Schema_Manager::create_tables()` auf und legt damit fehlende Tabellen an (u. a. `cbd_notes`/Fragenwand)**. Die Rückstellung auf `cbd_db_version = 2.9.0` bleibt, wird durch `create_tables()` aber sofort wieder auf `DB_VERSION` gehoben. Neue private Statics `get_plugin_tables()` und `table_exists()` — **eine neue Tabelle gehört hier UND in `CBD_Schema_Manager::create_tables()` UND in `$classroom_tables` in `admin/settings.php`** | `admin/*.php` |
| `class-cbd-ajax-handler.php` | AJAX-Endpunkte der Blockverwaltung | Speichern, Löschen, Standard-Design setzen. **Seit AP-1.2 aus `PLAN-PDF-Export-und-Tafelmodus-Fixes.md` (2026-08-25):** neue private Methode `sanitize_pdf_block_html($html)` — ersetzt an drei Stellen (`rest_generate_pdf()`, `generate_pdf()`, Legacy-Zweig) das direkte `wp_kses_post($html)` für PDF-Export-Blockinhalt. Maskiert `data:image/(png\|jpe?g\|gif\|webp);base64,...`-URIs vor `wp_kses_post()` per Platzhalter-Token (`@@CBD_DATA_URI_N@@`) und stellt sie danach wieder her — `wp_kses_post()` entfernt sonst das `data:`-Protokollpräfix (`wp_allowed_protocols()` enthält es nicht), wodurch eingebettete Notizen/Tafelbilder im PDF als mPDFs stiller 14×16-Fehler-Platzhalter landeten. Der naheliegende Fix über den Filter `kses_allowed_protocols` wirkt zur AJAX-/REST-Laufzeit **nicht** (`wp_allowed_protocols()` wendet ihn nur an, `if (!did_action('wp_loaded'))`) — empirisch verworfen. Muster wie `class-latex-parser.php::mask_protected_regions()` | – |
| `class-cbd-content-importer.php` | **Markdown-Parser** (Editor-Import) | `parse_markdown_content()` (public, wird auch vom Seitenimport genutzt), `ajax_parse_import_file`, `ajax_get_style_mappings`, `parse_accordion_directive()`, `markdown_to_html()`, `strip_unsafe_html()` | `CBD_TABLE_BLOCKS` |
| `class-cbd-block-serializer.php` | **Markdown-Abschnitte → `post_content`** (seit 3.1.86) | `html_to_blocks()`, `to_block_array()`, `to_post_content()`, `erzeuge_stable_id()`. Zielmarkup gemessen, siehe `tools/fixtures/` | `serialize_block()` (WP-Kern), `tools/fixtures/referenz-markup.html` als Vorlage |
| `class-cbd-page-importer.php` | **Seitenimport** (seit 3.1.86) | Menü unter dem Theme-Slug `page-manager` (Priorität 20), `ajax_titel_pruefen`, `ajax_seiten_importieren` (nutzt `wp_slash()`!). **Elternseite (seit Vorhaben „Importer-Elternseite"):** `bereinige_elternseite($roh): int` liest `$_POST['parent_id']` (`wp_unslash()` + `filter_var(..., FILTER_VALIDATE_INT)`, kein `(int)`-Cast) und reicht das Ergebnis als `post_parent` an `wp_insert_post()` weiter; jeder ungültige Wert fällt **still** auf `0` zurück, gilt für alle Dateien eines Laufs | `class-cbd-block-serializer.php`, `class-cbd-content-importer.php`, `admin/page-import.php` |
| `class-cbd-design-transfer.php` | Designs exportieren/importieren (Markdown und JSON) | `to_markdown()`, `parse_markdown()`, `parse_file()`, `normalize_designs()` | `admin/design-transfer.php` |
| `class-cbd-icon-library.php` | Eigene SVG-Kachel-Bibliothek | scannt zur Laufzeit das Dateisystem, `parse_stored_value()` (**einzige** Stelle, die das Speicherformat deutet), `flush_cache()` | `assets/icons/`, `uploads/cbd-icons/` |
| `class-cbd-icon-manager.php` | Upload-Seite für eigene Icons | schreibt nur nach `uploads/cbd-icons/`, `sanitize_icon_name()` | `class-cbd-svg-sanitizer.php`, `admin/icon-manager.php` |
| `class-cbd-svg-sanitizer.php` | SVG-Bereinigung per Whitelist | lehnt DOCTYPE/ENTITY ab (XXE), entfernt Skript, Event-Handler, fremde `xlink:href` | – |
| `class-latex-parser.php` | LaTeX-Formeln im Blockinhalt erkennen | `parse_latex()` auf `render_block` (Prio 5) **und `the_content` Prio 11 — nach `do_blocks()` und `wpautop()`** (seit 3.1.88; vorher Prio 5, was Absätze zerriss). Delimiter: `$$…$$`, `\[…\]`, `[latex]…[/latex]`, `$…$`, `\(…\)`. `render_display_formula()` gibt ein **`<span>`** aus, nie ein `<div>` — ein `<div>` in einem `<p>` spaltet den Absatz und kostet das Accordion Textknoten. **Code-Blöcke bleiben unangetastet (AP-1.fix2):** `KEIN_LATEX_BLOCK` überspringt `core/html`, `core/code`, `core/preformatted`, `core/freeform` — dort sind `\(` und `\[` JavaScript-Regexe, keine Formeln. Der Vergleich ist **strikt**, damit `blockName === null` (Freiform) durchläuft. Zusätzlich nimmt `mask_protected_regions()` `<script>`, `<pre>` und `<code>` per Platzhalter aus dem Text (**derselbe** Speicher wie die Formeln; Rücktausch in `restore_placeholders()`, auch im `catch`-Zweig). `normalize_formula_text()` dreht `<br />` und die wptexturize-Entities zurück und dekodiert **danach** HTML-Entities — **die Reihenfolge ist zwingend**, sonst wird `f'(x)` zu `f’(x)`. `should_load_katex()` nutzt `get_queried_object_id()` statt `get_post()` | `assets/js/latex-renderer.js`, `assets/css/latex-formulas.css`, `tools/test-latex-parser.php` |
| `class-latex-bulk-cleanup.php` | Reparatur beschädigter Formeln über alle Beiträge | – | `class-latex-parser.php` |
| `class-cbd-pdf-generator.php` | PDF serverseitig erzeugen | mPDF mit TCPDF-Rückfall. **Seit `PLAN-PDF-Export-und-Tafelmodus-Fixes.md` (2026-08-25):** `replace_css_variables()` nutzte zwei falsch benannte CSS-Variablen (`--color-primary-text`/`--color-light-background` statt real `--color-text-primary`/`--color-background-light`, griff dadurch nie) — behoben in AP-1.2, Ursache für dunklen Text auf dunklem Grund im Darkmode-Export. Neue Methode `ensure_download_htaccess($temp_dir)` (AP-1.3) legt beim ersten Export idempotent eine `.htaccess` mit `Content-Disposition: attachment` für `*.pdf` in `cbd-temp-pdfs/` an — defensive Ergänzung zur bereits korrekten `<a download>`-Technik in `pdf-server-side.js`, übersteuert aber keine vom Nutzer im eigenen Browser aktivierte „Vor jedem Download nachfragen"-Einstellung. `$mpdf->showImageErrors` ist seit AP-1.fix3 an `WP_DEBUG` gekoppelt statt dauerhaft `true` — bei aktivem Flag wirft mPDF bei jedem nicht dekodierbaren Bild eine `MpdfImageException` (`vendor/mpdf/mpdf/src/Image/ImageProcessor.php::imageError()`), die den **gesamten** Export abbricht statt nur das eine Bild durch einen stillen Platzhalter zu ersetzen — als reines Diagnosewerkzeug für AP-1.1 gedacht, nicht Teil der eigentlichen Bildkorrektur | `vendor/` |
| `class-cbd-classroom.php` | Klassenverwaltung, Zeichnungen, Schülerzugang | eigene Tabelle `cbd_classes`. **Geteilte Helfer (AP-2.1):** `zerlege_container_id()` — die **einzige** Stelle, die das Format `<stableId>:pN` deutet — `basis_container_id()` und `behandelte_container($class_id, $page_id)`. **Seit AP-2.1 aus `PLAN-PDF-Notizen-und-Listenformeln.md` (2026-08-24, nicht zu verwechseln mit dem gleichnamigen AP-2.1 der Helfer-Methoden oben):** neuer AJAX-Handler `ajax_get_page_drawings()` (Action `cbd_get_page_drawings`, registriert hinter `wp_ajax_cbd_load_drawing`), liefert für eine `page_id`+`class_id` ALLE Tafelbilder einer Seite in einem Aufruf (Bulk statt einem Request je Container) — Nonce `cbd_classroom_nonce` + Capability `cbd_edit_blocks` + `can_access_class($class_id)`, bewusst **nicht** das Token-Modell von `ajax_get_page_classroom_data()` (dort: Schüler-Sitzungen). Antwort `{drawings:[{container_id, drawing_data}, …]}`, Container mit `drawing_data IS NULL` werden serverseitig gefiltert. Grundlage für den PDF-Export in `pdf-server-side.js` **Vorhaben „Klassenverwaltung im Frontend" (3.1.115):** neue Konstante `SHORTCODE_LEHRER_KLASSEN = 'cbd_lehrer_klassen'` und fünf neue Methoden — `render_lehrer_klassen_shortcode()` (vier Zustände: System aus / nicht angemeldet / ohne `cbd_edit_blocks` / Oberfläche), `enqueue_lehrer_klassen_assets()` auf `wp_enqueue_scripts` (CSS sobald `has_shortcode()` greift, JS + Nonce erst bei vorhandener Capability), `render_lehrer_klassen_verwaltung()` (Markup-Zwilling von `admin/classroom.php`), `lehrer_klassen_seitendaten()` und `lehrer_klassen_seiten_url()`. Der Shortcode wird **vor** der `is_enabled()`-Weiche im Konstruktor registriert, anders als `[cbd_classroom]` — sonst stünde nach dem Abschalten des Klassen-Systems der rohe Shortcode-Text auf einer veröffentlichten Seite. **Rein additiv:** `ajax_save_class()`, `ajax_get_classes()`, `ajax_delete_class()`, `ajax_toggle_class_subscription()`, `render_classroom_shortcode()`, `enqueue_frontend_assets()`, `can_access_class()` und `get_teacher_classes()` sind byteidentisch geblieben (Hash-Vergleich gegen `HEAD`) — es entsteht keine neue Sicherheitsfläche, dieselben Endpunkte über eine andere Seite. Die Oberfläche lädt `assets/js/classroom-admin.js` **unverändert** mit; das Markup bringt dafür die drei Anknüpfungspunkte der Datei zeichengleich mit (Wrapper-Klasse `cbd-classroom-admin`, ein `<h1>` für `showNotice()`, alle Element-IDs). Details: `CLAUDE.md`, Abschnitt „Klassenverwaltung im Frontend: `[cbd_lehrer_klassen]`" **Seit „Klassenmodus-Live" AP-1.5 reiht `enqueue_frontend_assets()` in BEIDEN Zweigen (normale Seite mit `?classroom=`, Shortcode-Seite `[cbd_classroom]`) zusätzlich `cbd-klassenpuls` (`assets/js/klassenpuls.js`) ein — mit leerem Abhängigkeits-Array (kein jQuery, das Skript nutzt `fetch()`) — und lokalisiert `cbdKlassenpulsDaten` mit `restUrl` (`rest_url('cbd/v1/klassenpuls')`) und `takt` (`CBD_Klassenpuls::takt()`, nie die Option direkt gelesen). Beides steht hinter `class_exists('CBD_Klassenpuls') && CBD_Klassenpuls::takt() > 0` — bei Takt 0 wird gar nicht erst eingereiht, der Zustand bleibt dann byteidentisch zu vor diesem Vorhaben. Phase 1: Der Taktgeber läuft, aber es abonniert noch niemand (`classroom-page-filter.js` unverändert) | `admin/classroom.php`, `assets/css/lehrer-klassen.css`, `assets/js/lehrer-klassen.js`, `assets/js/classroom-admin.js`, `class-cbd-klassenpuls.php` |
| `class-cbd-fragenwand.php` | **Fragenwand: klassenspezifische Notizen** (neu mit Vorhaben „Fragenwand" AP-2.2, REST-Lesezugriff seit AP-2.3) | Singleton `CBD_Fragenwand` (Instanzierung am Dateiende wie `class-cbd-classroom.php`), fünf AJAX-Actions **nur für angemeldete Lehrpersonen**, bewusst ohne `wp_ajax_nopriv_*`-Gegenstück: `cbd_fragenwand_get_notes`, `_add_note`, `_toggle_note`, `_edit_note`, `_delete_note`. Guard-Kette je Methode: Nonce `cbd_classroom_nonce` → Capability `cbd_edit_blocks` → Parameter → Zugriffsprüfung → SQL (Muster von `CBD_Classroom::ajax_save_drawing()`). **Sicherheitskern:** bei den drei Einzelnotiz-Operationen liest `require_note_access()` die `class_id` IMMER aus der Datenbankzeile der Notiz, NIE aus `$_POST['class_id']` (Confused-Deputy-Schutz). `can_access_class()` ist eine bewusste, wortgleiche Kopie der gleichnamigen `private`-Methode aus `class-cbd-classroom.php` — die Bestandsklasse bleibt dafür unverändert. **Schüler-Lesezugriff (AP-2.3):** `register_rest_route()` meldet auf `rest_api_init` die Route `GET cbd/v1/fragenwand` an, `permission_callback => '__return_true'` — die gesamte Autorisierung steckt in `rest_get_notes_for_student()`: `nocache_headers()` als erste Zeile ohne Bedingung, dann `CBD_Classroom_Gate::sitzung()` (liest `?classroom=`/`?token=` selbst aus `$_GET`, prüft sie gegen den Transient `cbd_classroom_<token>`); die `class_id` stammt AUSSCHLIESSLICH aus der geprüften Sitzung, nie aus einem Request-Parameter. Jeder Fehlschlag liefert die zeichengleiche Ablehnung `fragenwand_ablehnung()` (`cbd_fragenwand_not_available`, HTTP 404) — Vorbild `CBD_Block_Content_API`. Antwort enthält je Notiz nur `id`, `text`, `ist_erledigt`. Bewusst **ohne** `args`-Deklaration der Route, damit ein unsinniges `?classroom=abc` nicht als HTTP-400-`rest_invalid_param` aus der Einheitlichkeit fällt. **Seit AP-2.fix1:** Text auf max. 5000 Zeichen begrenzt (`add_note`/`edit_note`, `mb_strlen()`), alle vier schreibenden Methoden prüfen den Rückgabewert von `$wpdb->insert()`/`query()`/`delete()` auf `false` statt unbedingt Erfolg zu melden — vorher erzeugte ein DB-Fehler bei überlangem Text eine Phantom-Notiz mit `id:0` bei gemeldetem `success:true`. Tabelle `CBD_TABLE_NOTES`, feste Sortierung `ist_erledigt ASC, created_at ASC, id ASC` in **beiden** Lesewegen. **Seit AP-3.1 zusätzlich Editor-Seite:** `register_editor_format()` auf `enqueue_block_editor_assets` meldet `assets/js/fragenwand-format.js` an **und reiht es ein** (Handle `FORMAT_HANDLE = 'cbd-fragenwand-format'`, Pfad in `FORMAT_SCRIPT`) — ein Textformat hängt an keinem Block, niemand würde das Handle sonst je einreihen (Vorbild `CBD_Inline_Reference::register_format_script()`). Abhängigkeiten von Hand: `wp-rich-text`, `wp-block-editor`, `wp-element`, `wp-i18n`, `wp-data` — kein Build-Schritt, also keine `index.asset.php`; `wp-rich-text` und `wp-data` stehen ausdrücklich da, obwohl `wp-block-editor` sie meist mitbringt. Fehlt die Datei, wird gar nichts registriert. Der Hook feuert nur im Editor, das Script erscheint nie im Frontend (per `curl` gegengeprüft). **Seit AP-3.2 zusätzlich die Frontend-Seite:** `enqueue_frontend_assets()` auf `wp_enqueue_scripts` reiht `assets/js/fragenwand-frontend.js` ein (Konstanten `FRONTEND_HANDLE`, `FRONTEND_SCRIPT`, `FRONTEND_DATA_OBJECT`) — **unbedingt auf jeder Frontend-Seite, ohne `has_block()`/Inhalts-Scan**, weil der Trigger ab Phase 4 auch außerhalb von `post_content` (Theme-Inhaltsverzeichnis) erscheinen kann. Eigener Methodenname trotz Wortgleichheit mit `CBD_Classroom::enqueue_frontend_assets()` — beide Klassen sind unabhängig. `wp_localize_script()` liefert `restUrl` (aus `rest_url(self::REST_NAMESPACE . self::REST_ROUTE)`, nicht abgeschrieben) und übersetzbare `texte`; **kein Nonce** (die Route ist sitzungsbasiert, `permission_callback => '__return_true'` — ein `wp_rest`-Nonce wäre für nicht angemeldete Schüler wertlos), **kein CSS** (folgt in AP-3.4). **Seit AP-3.fix1 trägt dieselbe Methode die Lehrer-Erkennung des Frontends:** Für angemeldete Nutzer mit `cbd_edit_blocks` ergänzt `lehrer_daten()` drei Schlüssel — `classes` (aus `teacher_classes()`, das `class_exists('CBD_Classroom')` **vor** `method_exists()` und dem Aufruf prüft und bei `null` ein leeres Array liefert), `ajaxUrl` und `nonce` (`cbd_classroom_nonce`, zeichengleich mit `class-cbd-block-registration.php:665-666`). Vorher hing die Erkennung im JS an `window.cbdClassroomData`, das `CBD_Block_Registration::enqueue_block_assets()` **nur auf Seiten mit Container-Block** ausgibt (`frontend_has_container_block()`) — auf einer Seite mit bloßem Fragenwand-Verweis fiel eine Lehrperson deshalb in den Schülerpfad. `class-cbd-block-registration.php` bleibt dabei unverändert; `ajaxUrl`/`nonce` mussten mit, sonst hätte die Klassenauswahl zwar erschienen, der anschließende AJAX-Aufruf aber keine Adresse gehabt. Bewusst **ohne** `CBD_Classroom::is_enabled()`-Prüfung (die Fragenwand-Hooks hängen ebenfalls nicht daran). **Seit AP-3.4 trägt `enqueue_frontend_assets()` zusätzlich das Frontend-CSS ein:** `assets/css/fragenwand.css` (Konstanten `STYLE_HANDLE = 'cbd-fragenwand'`, `STYLE_FILE`) wird **zuerst und unabhängig** vom Script-Zweig eingereiht (fehlt eine der beiden Dateien, reißt sie die andere nicht mit) — Version über `CBD_VERSION` (Fallback `false`, falls die Konstante fehlt). **`register_editor_format()` bekommt in AP-3.4 ein Gegenstück für den Editor:** Neben dem gewohnten `wp_enqueue_style(self::EDITOR_STYLE_HANDLE, ...)` für `assets/css/fragenwand-editor.css` registriert der Konstruktor zusätzlich `add_filter('block_editor_settings_all', array($this, 'inject_editor_style_into_iframe'))` — nötig, weil der Block-Editor seit WordPress 5.9 in einem eigenen `<iframe>` (`editor-canvas`) rendert, das ein gewöhnliches `wp_enqueue_style()` aus `enqueue_block_editor_assets` nicht erreicht; `block_editor_settings_all` ist der offizielle WordPress-Weg, `$settings['styles']` direkt mit dem Dateiinhalt zu füllen. Ohne diesen zweiten Weg blieb der eingefügte Verweis im Editor optisch nicht vom WordPress-Standardlink unterscheidbar (in AP-3.4 gefunden und behoben). `inject_editor_style_into_iframe(array $settings): array` trägt einen strikten Typhint auf einem WordPress-Filter — funktioniert, aber unüblich (geringer AP-3.rev-Befund, siehe `CLAUDE.md`). **Seit AP-4.2 zusätzlich die fünfte Naht zum Theme:** Der Konstruktor hängt `page_index_eintrag()` am leeren Theme-Filter `simple_clean_page_index_zusatzeintraege` ein (`Theme/includes/page-index.php`, AP-4.1) — umgekehrte Richtung zu den drei bestehenden Theme-Funktionsaufrufen, kein `function_exists()`-Guard nötig, da das Plugin hier selbst den Filter bedient statt eine Theme-Funktion aufzurufen. Liefert bei `?classroom=<id>` im Klassenmodus einen `<button class="cbd-fragenwand-verweis page-index__fragenwand-link">Fragenwand öffnen</button>` ganz oben im Inhaltsverzeichnis-Block; ohne den Parameter bleibt der Filterwert unverändert `''`. Erkennung bewusst nur clientseitig-plausibel (kein echter Sitzungscheck), die eigentliche Autorisierung passiert unverändert beim Klick über `cbd/v1/fragenwand`. **AP-4.fix1** entfernte ein ungültiges `href="#"` auf dem `<button>` und ergänzte die zuvor fehlende Gestaltung in `assets/css/fragenwand.css`. Details: `CLAUDE.md`, Abschnitt „Fragenwand", Unterabschnitt „Fünfte Naht zum Theme: Inhaltsverzeichnis-Eintrag" | `CBD_TABLE_NOTES`, `CBD_TABLE_CLASSES`, `CBD_Classroom_Gate::sitzung()`, `CBD_Classroom::get_teacher_classes()`, `assets/css/fragenwand.css`, `assets/css/fragenwand-editor.css`, Theme `includes/page-index.php` (Filter `simple_clean_page_index_zusatzeintraege`). **Vorhaben „Schüler-Fragen" (3.1.113):** beide Lesewege liefern je Notiz zusätzlich `ist_schueler_frage` (reines Herkunfts-Flag, KEINE Identität); der Schuelerweg zusätzlich einmalig `schuelerFragenErlaubt`. Neuer **POST-Endpunkt auf DERSELBEN Route** `cbd/v1/fragenwand` (`register_rest_route()` mit einem Array aus zwei Endpunkt-Definitionen), Callback `rest_add_note_from_student()` — Kette: `nocache_headers()` → Sitzung (Fehlschlag = dieselbe einheitliche 404 wie beim Lesen) → Text (leer/über 5000 Zeichen → 400) → Klassenschalter (→ 403) → `insert()` mit `teacher_id = 0` und `ist_schueler_frage = 1`. **Bewusst OHNE `wp_unslash()`** — anders als der AJAX-Weg: `WP_REST_Server` hat `$_POST` bereits entmaskiert, ein zweites `wp_unslash()` fräße echte Backslashes. Neuer privater Helfer `schueler_fragen_erlaubt(int $class_id)` |
| `class-cbd-classroom-gate.php` | **Klassen-Durchlass für gesperrte Seiten** (seit 3.1.87) | `sitzung()` (Transient entscheidet, nicht der URL-Parameter), `seite_freigeben()` bedient den Theme-Filter `simple_clean_lehrerseite_freigeben` (Standard `false`), `inhalt_reduzieren()` auf `the_content` **Priorität 8**, `block_erlaubt()` mit Rückfall auf `data-stable-id`. Alle Theme-Zugriffe über `function_exists()` | `class-cbd-classroom.php`, Theme-Funktionen |
| `class-cbd-klassenpuls.php` | **Klassenpuls: Signaturen für die Live-Aktualisierung** (neu mit Vorhaben „Klassenmodus-Live" AP-1.3) | Nur statische Methoden, kein Singleton. `init()` hängt `register_routes()` an `rest_api_init`; die Route `GET cbd/v1/klassenpuls?classroom=&token=[&page_id=]` läuft mit `permission_callback => '__return_true'` — die gesamte Autorisierung steckt in `liefere_puls()`: `nocache_headers()` als erste Anweisung ohne Bedingung, dann `CBD_Classroom_Gate::sitzung()` als **einzige** Token-Deutung; die `class_id` stammt ausschließlich aus der geprüften Sitzung, nie aus einem Request-Parameter. Antwort: `klasse`, `fragenwand`, immer — `seite`, `tafel` nur bei `page_id > 0` — plus `takt`. Jede Signatur 12 Zeichen `[0-9a-f]` aus `baue_signatur()` (`substr(md5(implode('|', …)), 0, 12)`, `null` → `''`, Reihenfolge zählt). Vier `$wpdb->prepare()`-Abfragen (Seite/Tafel gemeinsam, Klasse zweifach, Fragenwand); **kein `GROUP_CONCAT`** — dessen Längengrenze schnitte ab ca. 44 Containern still ab und fröre die Signatur ein. `takt()` ist die **einzige** Stelle, die die Option `cbd_klassenpuls_takt` auslegt (fehlt/unlesbar → 10, `<= 0` → 0 = abgeschaltet, sonst geklemmt auf 5…300) — AP-1.5/AP-1.6 rufen sie auf, statt Grenzen zu wiederholen. Ein abgeschalteter Puls ist **kein** Ablehnungsgrund (gültige Antwort mit `takt: 0`). Jeder Fehlschlag: zeichengleiche Ablehnung `ablehnen()` (`cbd_puls_not_available`, HTTP 404, `WP_REST_Response` wie `CBD_Block_Content_API::ablehnen()`). Prüfharnisch `tools/test-klassenpuls.php` (22 Prüfungen, ohne WordPress; Gruppe D wacht textbasiert gegen eine zweite Token-Deutung). Wird in AP-1.5 eingebunden. | `class-cbd-classroom-gate.php` (`sitzung()`), Konstanten `CBD_TABLE_DRAWINGS`/`CBD_TABLE_CLASS_PAGES`, optional `CBD_TABLE_NOTES` |
| `class-cbd-block-organizer.php` | Container-Blöcke zwischen Seiten kopieren/verschieben | – | `admin/block-organizer.php` |
| `class-cbd-block-reference.php` | Block „Block-Referenz" registrieren **und dessen Editor-Script anmelden**, dazu den gemeinsamen Auswahlbaustein | `EDITOR_HANDLE = 'cbd-block-reference-editor'`; `register_editor_script()` meldet `blocks/block-reference/index.js` von Hand an (Abhängigkeiten `wp-blocks, wp-element, wp-block-editor, wp-components, wp-i18n, wp-api-fetch`, Version über `filemtime()`). Nötig, weil es ohne Build-Schritt keine `index.asset.php` gibt — `block.json` nennt nur noch das Handle. **Beide Stellen zusammen ändern.** **`AUSWAHL_HANDLE = 'cbd-block-auswahl'` (AP-3.2):** `register_auswahl_script()` meldet `assets/js/block-auswahl.js` an und läuft in `register_block()` **vor** `register_editor_script()` — diese Reihenfolge ist Vertragsbestandteil. Die Abhängigkeit des Editor-Scripts wird nur gesetzt, **wenn** das Handle registriert ist (`wp_script_is(…, 'registered')`): Eine Abhängigkeit auf ein unbekanntes Handle lässt WordPress das Editor-Script stillschweigend gar nicht ausgeben — bei fehlender Datei wäre der Block sonst komplett verschwunden statt nur seiner Auswahl | `blocks/block-reference/`, `assets/js/block-auswahl.js` |
| `class-cbd-block-content-api.php` | REST-Route `cbd/v1/block-html` (AP-2.4): gerendertes HTML **eines einzelnen** Container-Blocks für das Modal der Block-Referenz. **Bewusst eine eigene Klasse** neben `class-cbd-blocks-rest-api.php` — dort gilt „nur Redakteure", hier „jeder, aber nur was er sehen darf" | `permission_callback` = **`'__return_true'`**, die gesamte Autorisierung leistet der Callback: `nocache_headers()` **immer zuerst** → Typ und `publish` → `post_password_required()` → `simple_clean_seite_sichtbar()` **hinter `function_exists()`** (nicht `…nur_lehrpersonen()`, die kennt den Klassen-Durchlass nicht) → auf gesperrten Seiten `CBD_Classroom_Gate::sitzung()` und `CBD_Classroom::behandelte_container()`. Verlangt `post_id` **und** `stable_id`; die Rechteprüfung hängt an `post_id`, weil `copy_block()` die `stableId` **nicht** regeneriert. Gerendert mit **`render_block()`**. Antwort `{html, title}` mit dem **Blocktitel** — nie Seitentitel oder Permalink. **Jeder** Fehlschlag liefert dieselbe 404 `cbd_block_not_available`; unterschiedliche Antworten ließen sich zum Kartieren gesperrter Lösungsseiten durchprobieren | `class-cbd-classroom-gate.php`, `class-cbd-classroom.php`, Theme `includes/sichtbarkeit.php` (nur lesend), `tools/test-block-content-api.php` |
| `class-cbd-inline-reference.php` | **Serverseite des Inline-Verweises (AP-3.3)** — der Blockreferenz als **Textformat** statt als Block. Zwei Aufgaben: der `the_content`-Filter, der das gespeicherte `<a>` auffrischt, und die Registrierung des Format-Scripts | `inhalt_auffrischen()` auf `the_content` **Priorität 12** (nach dem LaTeX-Netz auf 11, weit vor der Glossar-Autoverlinkung auf 10000). **Zwei Wächter vor jeder Arbeit:** fehlt die Klassenzeichenkette im Inhalt → unverändert zurück; fehlt `WP_HTML_Tag_Processor` (WordPress < 6.2) → unverändert zurück, der Verweis bleibt ein gewöhnlicher Link. **Kein regulärer Ausdruck auf Markup.** Der Filter setzt `data-display-mode="modal"`, `aria-haspopup="dialog"`, `data-same-page` nur auf der eigenen Seite (sonst **entfernt**) und frischt `href` aus `get_permalink()` auf — **serverseitig, weil ein Textformat seine Attribute beim Bearbeiten einfriert**: `href` verrottete nach einer Slug-Änderung, `data-same-page` zeigte nach dem Kopieren eines Absatzes auf den falschen Zwilling, und `aria-haspopup` steht nicht in der ARIA-Whitelist von kses und würde einem Block-Redakteur beim Speichern entfernt. Liefert `get_permalink()` nichts, bleibt der gespeicherte `href` stehen. `register_format_script()` auf `enqueue_block_editor_assets` **registriert und reiht ein** — ein Textformat hängt an keinem Block, niemand würde das Handle sonst je einreihen. Das View-Script wird **aus dem Inhaltsfilter** eingebunden, nicht über einen zweiten Inhalts-Scan; das trägt nur, weil `view.js` ein **Footer-Script** ist (Warnung steht im Code). **`ziel_post_id()` validiert mit `ctype_digit()` plus `filter_var(..., FILTER_VALIDATE_INT)`**, nicht mit einem nackten `(int)`-Cast (AP-3.fix2) — Letzterer hätte ab PHP 8.1 bei zwanzigstelligen Ziffernfolgen eine Warnung erzeugt und den Wert auf `PHP_INT_MAX` verstümmelt, statt das Element unangetastet zu lassen; führende Nullen (`045`) bleiben bewusst abgelehnt, nicht normalisiert (AP-3.fix5), weil der Wert aus der echten Zielauswahl ohnehin nie so aussehen kann. `ziel_href()` verweist im Kommentar auf `render.php` **und** `blocks/block-reference/format.js` als die beiden anderen Fassungen derselben URL-Bildungsregel (AP-3.fix5, AP-4.fix1) — kein automatischer Wächter, weil er rendern müsste | `blocks/block-reference/view.js`, `blocks/block-reference/format.js`, `class-cbd-block-reference.php` (`AUSWAHL_HANDLE`), `tools/test-inline-reference.php` |
| `class-cbd-blocks-rest-api.php` | **Zwei** Redakteurs-Routen für die Zielauswahl: `cbd/v1/blocks` liefert die Container-Block-**Instanzen** aller veröffentlichten Beiträge und Seiten (**nicht** die Block-Designs), `cbd/v1/seitenbaum` (AP-3.1) den Seitenbaum für die hierarchische Filterung | `permission_callback` = `edit_posts` für **beide** Routen — gleiches Sicherheitsmodell, deshalb dieselbe Klasse; der öffentliche Endpunkt liegt bewusst getrennt in `class-cbd-block-content-api.php`. `extract_stable_id()`: erst Attribut `stableId`, dann Rückfall auf `data-stable-id` im gespeicherten HTML über `WP_HTML_Tag_Processor` — bewusst **kein dritter Regex** neben `class-cbd-classroom-gate.php` und `class-cbd-block-registration.php`; auf WordPress < 6.2 entfällt nur der Rückfall. Antwort `cbd/v1/blocks` (nackte **Liste**, seit AP-3.1 um die letzten drei Felder erweitert): `stableId`, `anchor`, `blockId` (Legacy), `blockTitle`, `postId`, `postTitle`, `postUrl`, `blockType`, `postParent`, `menuOrder`, `postType`. **Antwortform bewusst nicht in ein Objekt verpackt** — der Baum kam deshalb als eigene Route. Antwort `cbd/v1/seitenbaum`: `{knoten, kinder, wurzeln}`. Der Baum wird mit rohem `$wpdb` und **fünf Spalten ohne `post_content`** geladen und per **Breitensuche ab Wurzel 0** aufgebaut (Vorbild `Theme/includes/page-index.php:206-229`): Das liefert `tiefe` ohne erneutes Auflösen der Elternkette, lässt verwaiste Knoten samt Unterbaum herausfallen und macht Zyklen unerreichbar. `baue_seitenbaum()` ist von der Abfrage getrennt und damit ohne WordPress testbar. Das Feld `gesperrt` kommt aus dem Theme, **hinter `function_exists()`**, und über die memoisierte Karte `simple_clean_gesperrte_seiten_mit_unterbaum()` statt einer Prüfung je Seite (AP-3.fix1) — die Variante je Seite läuft in `get_post_ancestors()` und hätte auf 258 Seiten hunderte Einzelabfragen erzeugt, weil die rohe Abfrage den Post-Cache nicht füllt. **Aus AP-3.fix3 nachgezogen:** `knoten` und `kinder` werden vor der Ausgabe explizit auf `(object)` gecastet — ein PHP-Array mit den Schlüsseln `0..n-1` ginge sonst als JSON-**Liste** heraus und würde vom Client stillschweigend verworfen; `update_meta_cache()` läuft nur noch in der Rückfallstufe (in der bevorzugten Stufe überflüssig, weil die Theme-Funktion dort keine Meta liest); `orderby` steht wieder auf `title` (der Wechsel auf `menu_order title` in AP-3.1 verschlechterte nur die flache Trefferliste der Suche, ohne der über `kinder` laufenden Baumreihenfolge zu nutzen). **Seit „Seitenimporter-Kaskaden-Zielauswahl" (2026-08-25):** `get_seitenbaum()` liest zusätzlich den optionalen Parameter `entwuerfe` (`'1'` schließt `post_status = 'draft'` zusätzlich zu `publish` ein; Fehlen oder jeder andere Wert bleibt exakt beim bisherigen Nur-`publish`-Verhalten — reine Opt-in-Erweiterung, `block-auswahl.js` ruft die Route weiterhin ohne diesen Parameter auf). `self::$seitenbaum_cache` ist dafür von einem einzelnen `WP_REST_Response|null`-Slot auf ein Array mit den Schlüsseln `'ohne_entwuerfe'`/`'mit_entwuerfe'` umgestellt (verhindert Vermischen zweier Parametervarianten innerhalb derselben Anfrage); `seitenbaum_cache_vergessen()` leert beide gemeinsam. Konsumiert vom Seitenimporter, siehe `page-importer.js` unten | `blocks/block-reference/index.js`, `assets/js/block-auswahl.js`, `assets/js/page-importer.js`, `class-cbd-classroom-gate.php`, Theme `includes/sichtbarkeit.php` (nur lesend), `tools/test-seitenbaum.php` |
| `class-cbd-migration.php` | Stable-IDs für bestehende Container nachtragen | – | `admin/migration.php` |
| `functions.php` | Freistehende Hilfsfunktionen | Icon-Größe: `cbd_icon_scale_bounds()`, `cbd_sanitize_icon_scale()`, `cbd_get_icon_scale_css()`. **Klassenpuls-Takt (AP-1.6 von `PLAN-Klassenmodus-Live.md`):** `cbd_sanitize_klassenpuls_takt($wert): int` — nach dem Vorbild von `cbd_sanitize_icon_scale()`; nicht numerisch → 10, `<= 0` → 0 (Klassenpuls aus), sonst geklemmt auf 5…300. Bereinigt die Option `cbd_klassenpuls_takt` beim Speichern in `admin/settings.php`; gelesen wird sie (bereits bereinigt) von `CBD_Klassenpuls::takt()` (AP-1.3). Icon-Wert: `cbd_sanitize_icon_value()`, `cbd_parse_features_from_post()`. **Icon-Position (AP-2.1):** `cbd_icon_position_defaults()` (fünf Positionen, Vorgabe `header`, Grenzen −200/200), `cbd_sanitize_icon_position()` — **die Altwerte `top-left`…`bottom-right` fallen auf `header` zurück**, sonst verlöre jedes Bestandsdesign sein Kopfzeilen-Icon —, `cbd_sanitize_icon_offset()` (deutsches Dezimalkomma, Rundung, Klemmung), `cbd_get_icon_position_class()`, `cbd_get_icon_position_style()` (**nie ein Dezimalkomma**), `cbd_icon_position_preview()`. `cbd_parse_features_from_post()` schreibt zusätzlich `icon.position`, `icon.offsetX`, `icon.offsetY` — **flach**, weil `CBD_Design_Transfer` mit flachen Punkt-Pfaden serialisiert | `tools/test-icon-position.php` |
| `user-capabilities.php` | Eigene Rechte | `cbd_edit_blocks`, `cbd_edit_styles`, `cbd_admin_blocks` | – |
| `php8-wordpress-compatibility.php` | Dämpft Deprecation-Warnungen von WP-Core unter PHP 8 | – | – |

## Datenbank (`includes/Database/`)

| Datei | Zweck | Wichtige Funktionen/Inhalte | Hängt ab von |
|---|---|---|---|
| `class-schema-manager.php` | Schema und Migrationen | `DB_VERSION` (3.1.61, seit Vorhaben „Fragenwand" AP-2.1 bewusst NICHT erhöht), `CREATE TABLE` für `cbd_blocks`, `cbd_classes`, `cbd_class_pages`, `cbd_drawings` und seit AP-2.1 zusätzlich `cbd_notes` (klassenweite Fragenwand-Notizen, kein `page_id`/`container_id`-Bezug, Sortierung rein per Abfrage statt eigener Reihenfolge-Spalte), `run_migrations()`. **Legt `slug` NICHT an** und benennt eine vorgefundene `slug`-Spalte nach `name` um — während `CBD_Admin::handle_database_repair()` `slug` als Pflichtspalte führt. Bekannte Unstimmigkeit, siehe `docs/archiv/PLAN-Seitenimport.md`, AP-1.0.fix1 | –. **Vorhaben „Schüler-Fragen" (3.1.113):** neue Spalten `cbd_classes.schueler_fragen_erlaubt` und `cbd_notes.ist_schueler_frage` in den `CREATE TABLE`-Strings (greift nur bei Neuinstallationen) PLUS drei neue Statics: **`get_required_columns()`** (die eine Liste nachgereichter Spalten), **`ensure_columns()`** (`SHOW COLUMNS` je Spalte → `ALTER TABLE ADD COLUMN`, idempotent, am Ende von `create_tables()` aufgerufen) und **`get_missing_columns()`** (nur für die Anzeige). **Warum von Hand:** `dbDelta()` zieht neue Spalten auf bestehenden Tabellen NICHT nach — `CREATE TABLE IF NOT EXISTS` unterläuft sein Namensregex, es hält `IF` für den Tabellennamen (empirisch: Rückgabe `array('IF' => 'Created table IF')`, Spalten unverändert). Siehe `CLAUDE.md`, Abschnitt „Schüler dürfen Fragen stellen" |

## Admin-Ansichten (`admin/`)

| Datei | Zweck |
|---|---|
| `dashboard.php` | Einstiegsseite des Plugins |
| `blocks-list.php` | Übersicht aller Block-Designs |
| `new-block.php` / `edit-block.php` | Design anlegen und bearbeiten |
| `settings.php` | Einstellungen, u. a. Icon-Größe. Enthält außerdem die Karte „Datenbank reparieren" mit der Schaltfläche „Alle Migrationen durchführen" (`CBD_Schema_Manager::create_tables()`). **`$classroom_tables` steuert, ob diese Schaltfläche überhaupt gerendert wird** — bis 3.1.110 fehlte `cbd_notes` in der Liste, dadurch blieb `$needs_migration` falsch und die Schaltfläche unsichtbar, obwohl die Fragenwand-Tabelle fehlte. **Seit AP-1.6 von `PLAN-Klassenmodus-Live.md`:** direkt unter „Klassen-System" ein neues Feld „Live-Aktualisierung (Sekunden)" (`<input type="number" name="klassenpuls_takt">`), gespeichert über `cbd_sanitize_klassenpuls_takt()` (`includes/functions.php`) in die Option `cbd_klassenpuls_takt` — die Notbremse aus Abschnitt 5 des Plans (0 = Klassenpuls aus) |
| `icon-manager.php` | Eigene Icons hochladen |
| `design-transfer.php` | Designs exportieren/importieren |
| `page-import.php` | **Seiten aus Markdown importieren** (seit 3.1.86). **Seit „Seitenimporter-Kaskaden-Zielauswahl" (2026-08-25):** Das frühere `wp_dropdown_pages()` ist entfernt; die Elternseiten-Auswahl `cbd-import-parent` ist jetzt ein verstecktes `<input type="hidden">` (unverändert dieselbe ID/dasselbe Name-Attribut), daneben ein leerer Container `#cbd-pi-kaskade`, den `page-importer.js` per Kaskade füllt — Entwürfe bleiben wie zuvor wählbar, jetzt über den REST-Parameter `entwuerfe=1` statt über den `post_status`-Filter von `wp_dropdown_pages()` |
| `block-organizer.php` | Blöcke zwischen Seiten verschieben |
| `classroom.php` | Klassenverwaltung |
| `migration.php` | Stable-ID-Migration |
| `import-export.php` | **Nicht verlinkt** — in v3.1.50 abgeschaltet, durch `design-transfer.php` ersetzt. Liegt nur noch im Repo |

## Blöcke (`blocks/`)

| Datei | Zweck |
|---|---|
| `block-reference/block.json` | Blockdefinition `cbd/block-reference`, apiVersion 3, Kategorie **`container-blocks`** (so heißt die in `container-block-designer.php` registrierte Kategorie — `container` wäre unbekannt und erzeugte eine Warnung). Attribute: `targetStableId`, `targetAnchor`, `targetBlockId` (Legacy, **nicht entfernen**), `targetPostId`, `targetBlockTitle`, `targetPostTitle`, `linkText`, `showIcon`, **`displayMode`** (AP-2.5; `modal` oder `link`, **Vorgabe `modal`**). **Rückwärtskompatibilität beachten:** Ein gespeicherter Block **ohne** dieses Attribut bekommt `modal` und öffnet ein Overlay, statt zu springen — das Markup bleibt gültig, aber das Verhalten bestehender Seiten ändert sich. `editorScript` ist ein **Handle**, kein `file:`-Pfad. **Muss im ZIP enthalten sein** — fehlte bis v3.1.76 und existierte deshalb auf Produktivinstallationen nicht |
| `block-reference/index.js` | Editor-Ansicht (Seitenleiste). **Kein Build-Schritt:** IIFE, `wp.*`-Globale, `wp.element.createElement` — kein JSX, kein `import`/`export`. **Seit AP-4.1** liefert `window.cbdBlockAuswahl.HierarchieAuswahl` (Vertrag C) die Zielauswahl samt eigenem Suchfeld und Laden — das frühere flache `SelectControl` mit eigenem `useEffect`-Ladeblock, `schluessel()`, `passtZurSuche()` und dem Optionsaufbau ist entfernt. `index.js` behält nur noch, was zum Block selbst gehört: Link-Text, Icon-Schalter, `displayMode`, sowie einen Wächter, der bei fehlendem `window.cbdBlockAuswahl` eine `Notice` statt eines Absturzes zeigt. Listenschlüssel bleibt `postId|stableId` (jetzt über `window.cbdBlockAuswahl.schluessel()`), weil `CBD_Block_Organizer` die `stableId` beim Kopieren nicht neu vergibt |
| `block-reference/format.js` | **Inline-Textformat** `cbd/block-reference-inline` (AP-4.2, seit 3.1.93) — derselbe Verweis, aber **mitten im Text** statt als eigener Block. `registerFormatType` mit `tagName: 'a'` (die Glossar-Autoverlinkung des Themes überspringt `<a>` korrekt; bei `span` setzte sie ein zweites `<a>` hinein) und eigener Klasse `cbd-block-reference-inline` statt der Block-Klasse (die trägt `display:block` samt Kartenlayout und zerrisse den Textfluss). `RichTextToolbarButton` neben dem Link-Knopf, Dialog über `window.cbdBlockAuswahl.HierarchieAuswahl`. **`linkImBereich()` prüft jedes Zeichen der Markierung** auf ein `core/link`-Format statt `getActiveFormat()` zu befragen (das nur anschlägt, wenn ein Format die **ganze** Markierung überspannt) — sonst entstehen verschachtelte `<a>` im gespeicherten Inhalt (AP-4.fix2, Befund B1, **der einzige Schaden dieses Vorhabens, der sich nicht durch ein Update reparieren ließe**). Trägt die Klassenzeichenkette `cbd-block-reference-inline` **dreifach** (zwei Docblocks + der wirksame Wert) — nur `tools/test-inline-reference.php` Gruppe 11 prüft den **wirksamen Ausdruck**; ein reines `strpos()` bliebe bei einer Mutation des Werts still grün, solange die Kommentare stehen bleiben (AP-4.fix2, Befund B2). Registriert und **eingereiht** von `class-cbd-inline-reference.php::register_format_script()` (ein Textformat hängt an keinem Block, niemand würde das Handle sonst je einreihen); Abhängigkeiten u. a. `wp-rich-text`, `wp-data` (AP-4.fix1, Befund F1 — war zunächst nicht deklariert) | `class-cbd-inline-reference.php`, `assets/js/block-auswahl.js`, `blocks/block-reference/view.js`, `blocks/block-reference/style.css` |
| `block-reference/render.php` | Serverseitiges Markup. Sprungmarke: `targetAnchor` → `#fragment`; sonst Parameter `?cbd-ref=<stableId>`; Altbestand ohne `stableId` behält `#<targetBlockId>`. Gibt `data-target-stable-id`, `data-target-anchor`, `data-target-post`, `data-same-page` aus. Ohne Ziel oder ohne `targetPostId` **keine** Ausgabe. Teilt sich die URL-Bildungsregel (Anker gewinnt gegen `cbd-ref`) mit `CBD_Inline_Reference::ziel_href()` **und** `blocks/block-reference/format.js` — an allen drei Stellen nur per Kommentar verbunden, kein automatischer Duplikatswächter (der müsste rendern) |
| `block-reference/view.js` | Frontend: weiches Scrollen und kurze Hervorhebung (`.cbd-block-reference-highlight`, 2 s). Zielsuche `getElementById(anchor)` → `[data-stable-id="…"]` mit `CSS.escape` und Rückfall. Wertet beim Laden Fragment **und** `?cbd-ref=` aus. **Seit AP-4.2 genau eine geänderte Zeile** (`git diff vor-phase-4..HEAD` liefert `1 1`): Der Klick-Selektor fürs Modal erkennt jetzt zusätzlich `.cbd-block-reference-inline` neben `.cbd-block-reference-link` — der Inline-Verweis nutzt damit dieselbe Modal-Logik (DOM-Klon, Nachladen, Fokusfalle) wie der Block |
| `block-reference/style.css` + `editor.css` | Optik des Verweises und der Editor-Vorschaukarte. Die früher zu breite Regel `[id] { scroll-margin-top }` ist auf `.cbd-container, .cbd-container-block` eingegrenzt — beides sind mögliche Sprungziele (`data-stable-id` außen, Anker-`id` innen). **Seit AP-4.2** trägt `style.css` zusätzlich `.cbd-block-reference-inline` (fünf Selektoren, AP-4.fix2 Befund B3): `display: inline` mit Unterstreichung statt des Kartenlayouts von `.cbd-block-reference-link`, dazu ein `::after`-Pfeil (`\2197`) als `inline-block` (sonst zieht der Vorfahre die Unterstreichung nicht über das Symbol). **Seit 3.1.94 folgt die Karte des Blocks dem Knopf-Farbschema der Website** statt des mitgelieferten Violett-Verlaufs (`#667eea`/`#764ba2`), das mit dem Projekt nie etwas zu tun hatte: plastischer Look der Icon-Kacheln, Farbstufen über `color-mix()` aus `--color-ui-surface` abgeleitet und **nicht** aus den `--plastic-*`-Variablen des Themes geholt — das Plugin muss auch unter einem anderen Theme tragen (gleiches Muster wie `assets/css/classroom-frontend.css`). **`background-image`, nie die Kurzschreibweise `background`:** Wäre der Verlauf ungültig (Browser ohne `color-mix()`), setzte die Kurzschreibweise auch `background-color` zurück und die Karte wäre durchsichtig statt einfarbig. **Der Hover tauscht den ganzen Verlauf aus**, nicht nur `background-color` — sonst überdeckten die Verlaufsschichten die neue Farbe und beim Überfahren passierte nichts (dieselbe Falle steht in `assets/js/floating-pdf-button.js`). Die Hervorhebung des angesprungenen Blocks (`@keyframes cbdBlockHighlight`) läuft ebenfalls über die UI-Farbe. `editor.css` bleibt bewusst in WordPress-Admin-Blau: Es stylt eine Infotafel im Canvas, keine Nachbildung der Karte. Damit steht die Klassenzeichenkette an **vier** Stellen im Projekt (hier plus `class-cbd-inline-reference.php`, `format.js`, `view.js`) — anfangs war `style.css` von keinem Wächter erfasst. **Seit AP-2.8 (Phase 2 CSS-Variablen, 3.1.100):** die letzten 5 freistehenden Literale in `style.css` (Kartentext/Modal-Hintergrund `#fff`, Schließen-Button-Text `#333`, Lade-/Fehlertext `#666`) auf `var(--color-background, #fff)`, `var(--color-text-primary, #333)` bzw. `var(--color-text-muted, #666)` umgestellt — alle fünf trafen exakt den Live-Wert der Variable, keine Ausnahme nötig. Die `--color-ui-surface`-Kopplung und die `color-mix()`-Kette blieben unangetastet |

## JavaScript (`assets/js/`)

Alle 22 Dateien sind eingebunden (geprüft 2026-08-10; seit Vorhaben
„Fragenwand" kommen `fragenwand-format.js` (AP-3.1) und
`fragenwand-frontend.js` (AP-3.2) dazu, seit „Klassenverwaltung im
Frontend" zusätzlich `lehrer-klassen.js`). Kein Build-Schritt —
IIFE, Zugriff über `wp.*`-Globale, `console.log` hinter `window.cbdDebug`.

| Datei | Zweck |
|---|---|
| `block-editor.js` | Registriert `container-block-designer/container`. **`ContainerBlockSave` ab Zeile 319 ist die Vorlage für den Serializer**; `generateId()` Zeile 83 erzeugt die `stableId` |
| `content-importer.js` | Import-Dialog im Editor; `insertBlocks()` ab Zeile 309 ist das Referenzverhalten des Serializers |
| `page-importer.js` | Oberfläche der Importseite (Dateiauswahl, Stil-Dialog, Fortschritt). Liest die gewählte Elternseite **einmal vor dem Lauf** (`importStarten()`) und schickt sie bei jedem der Aufrufe je Datei als `parent_id` mit; das versteckte Feld `#cbd-import-parent` ist während des Laufs `disabled`. **Seit „Seitenimporter-Kaskaden-Zielauswahl" (2026-08-25):** Abschnitt „Elternseite: gestaffelte, kaskadierende Auswahl" — `kaskadeLaden()` holt beim Laden über `window.wp.apiFetch({ path: '/cbd/v1/seitenbaum?entwuerfe=1' })` den erweiterten Seitenbaum, `kaskadeZeichnen()`/`kaskadeEbeneBauen()` bauen daraus je Ebene ein `<select class="cbd-pi-kaskade-ebene">` (Titel ausschließlich über `option.text`, nie `innerHTML`), `kaskadeAuswahlGeaendert()` hängt bei einer Seite mit Kindern die nächste Ebene an und aktualisiert `#cbd-import-parent` synchron, `kaskadeFehler()` fällt bei einem fehlgeschlagenen Abruf auf „oberste Ebene" zurück, `kaskadeSperren()` deaktiviert alle sichtbaren Ebenen während des Laufs zusätzlich zum bestehenden Feld. **Fix AP-1.fix1 (Review-Befund B1):** `kaskadeAuswahlGeaendert()` brach ursprünglich nur bei `gewaehlteId === 0` ab und hängte beim Rücksprung auf „— diese Seite als Elternseite —" (Wert = eigene Eltern-ID, nicht `0`) eine wortgleiche Doppel-Ebene an; der Fix vergleicht zusätzlich gegen `ausgewaehltesFeld.options[0].value`. Regressionsabdeckung: `tools/test-page-importer-kaskade.js` |
| `block-auswahl.js` | **`window.cbdBlockAuswahl` (AP-3.2)** — die hierarchische Zielauswahl, geteilt von der Seitenleiste des Blocks „Block-Referenz" und (ab AP-4.2) vom Inline-Textformat. **Erste öffentliche `window.cbd*`-Schnittstelle für den Editor**, sonst gibt es die nur im Frontend. Sieben Namen: `ladeDaten` (lädt `cbd/v1/blocks` und `cbd/v1/seitenbaum` parallel, **memoisiert** — mehrere Aufrufer teilen ein Promise, lehnt nie ab), `schluessel`, `text`, `passtZurSuche`, `pfadVon`, `ebenen`, `HierarchieAuswahl`. **`wp.element` wird beim Laden der Datei nicht berührt**, nur in der Komponente — nur so ist die reine Logik ohne WordPress testbar. Zweige ohne erreichbaren Zielblock sind beschnitten; alles, was nicht im Seitenbaum steht, sammelt die Wurzeloption „Beiträge", damit bei einem Baumausfall kein Ziel unerreichbar wird. Gesperrte Seiten werden **gekennzeichnet, nicht ausgeblendet**. **Seit AP-3.fix4** meldet die Blockstufe bei einem gelöschten Zielblock kein `melde(null)` mehr, solange der zugehörige Eintrag noch als aktueller Wert gilt, aber nicht mehr in `bloecke` steht — zuvor hätte die Ersatzoption „(gespeichertes Ziel)" das gespeicherte Ziel beim Anklicken gelöscht, obwohl Vertrag C `null` nur beim ausdrücklichen Abwählen vorsieht. Registriert in `class-cbd-block-reference.php` (`AUSWAHL_HANDLE`), Daten aus `class-cbd-blocks-rest-api.php`, Tests in `tools/test-block-auswahl.js` |
| `fragenwand-format.js` | **Inline-Textformat `cbd/fragenwand-verweis`** (Vorhaben „Fragenwand" AP-3.1) — markierter Text wird zu `<a class="cbd-fragenwand-verweis" href="#">…</a>`, das im Frontend die Fragenwand der aktuellen Klasse als Modal öffnet (AP-3.2). Strukturvorbild ist `blocks/block-reference/format.js`, aber **stark vereinfacht**: Es gibt genau EINE Fragenwand je Klasse, also kein Ziel auszuwählen — kein `Modal`, kein `wp.components`, kein `useState`, keine Abhängigkeit von `window.cbdBlockAuswahl`, **kein** `data-target-*`. Der Werkzeugleisten-Knopf (`RichTextToolbarButton`, Dashicon `sticky`) wendet das Format unmittelbar an bzw. entfernt es. Gespeichert wird **genau ein Attribut**, `href="#"` — rein strukturell, es gibt keine sinnvolle Sprungziel-URL; daher auch **kein** serverseitiger Auffrisch-Filter analog `CBD_Inline_Reference::inhalt_auffrischen()`. **`linkImBereich()` prüft jedes Zeichen der Markierung** auf ein `core/link` (nicht `getActiveFormat()`, das nur bei voller Überspannung anschlägt) und verweigert dann die Anwendung mit einer `core/notices`-Snackbar — sonst entstünden verschachtelte `<a>` im gespeicherten `post_content`, der einzige Schaden dieser Art, den kein Update zurückholt. **Der Deaktiviert-Zustand steht zweimal:** `disabled` (liest `ToolbarButton`) **und** `isDisabled` (liest `DropdownMenu`) — ein Format ohne `name`-Prop landet in der Überlaufliste „Mehr", und die rendert die Fills nicht, sondern reicht nur deren Props als `controls` weiter; ohne `isDisabled` bliebe der Eintrag dort immer anklickbar (live gemessen gegen WordPress 7.0.4). Registriert und **eingereiht** von `CBD_Fragenwand::register_editor_format()` auf `enqueue_block_editor_assets` | `class-cbd-fragenwand.php`, (ab AP-3.2) `assets/js/fragenwand-frontend.js`, `assets/css/fragenwand.css` |
| `fragenwand-frontend.js` | **Frontend-Gegenstück des Verweises: Modal, Trigger-Delegation, Datenabruf** (Vorhaben „Fragenwand" AP-3.2; Lehrer-Klassenauswahl und Verwaltungscontrols seit AP-3.3). Ein **delegierter Klick-Listener auf `document`** fängt jeden Klick auf `.cbd-fragenwand-verweis` ab — bewusst nicht an den Verweisen selbst, weil der Trigger sowohl aus dem Textformat im `post_content` (AP-3.1) als auch ab Phase 4 aus dem Theme-Inhaltsverzeichnis stammen kann. `href="#"` wird per `preventDefault()` entschärft; modifizierte Klicks (Strg/Cmd/Shift/Alt, mittlere Maustaste) bleiben dem Browser überlassen. **Öffentliche Schnittstelle `window.CBDFragenwand`** mit `open(classId, notes, optionen)`, `openMitMeldung(text, art)`, `close(fokusZurueck)` und (seit AP-3.3) `lehrerFlow(trigger)`; ein vorhandenes Objekt wird ergänzt statt ersetzt. Ist das Feld `cbdFragenwandFrontend.classes` vorhanden (Lehrer-Hinweis; **seit AP-3.fix1** von `CBD_Fragenwand::enqueue_frontend_assets()` gesetzt — bis dahin `window.cbdClassroomData`, das `class-cbd-block-registration.php` nur auf Seiten **mit Container-Block** im `wp_footer` ausgibt und das auf einer Seite mit bloßem Fragenwand-Verweis fehlte; die bloße Existenz des Feldes zählt, eine leere Liste bedeutet weiterhin „Lehrperson"), ruft `oeffneAusTrigger()` `window.CBDFragenwand.lehrerFlow(trigger)` auf, sofern vorhanden — sonst nur ein `console.warn`, kein Modal. **Lehrer-Weg (AP-3.3):** `lehrerFlow()` zeigt IMMER zuerst `zeigeKlassenauswahl()` — eine **eigene, neue** Funktion nach dem optischen/strukturellen Vorbild von `showClassSelector()` in `assets/js/board-mode.js` (Overlay + Dialog + ein Knopf je Klasse + Abbrechen + Backdrop-Klick + Escape), aber mit eigenen Klassennamen `cbd-fragenwand-klassenwahl-*`, per `document.createElement` statt `innerHTML` und **ohne die dortige Option „Persönlich (lokal)"** (`data-class-id="0"`): Die Fragenwand kennt keinen lokalen Modus. `board-mode.js` wird dabei weder geändert noch aufgerufen (Plan-Vorgabe, Risikoregister). Leere `cbdFragenwandFrontend.classes` (seit AP-3.fix1; vorher `cbdClassroomData.classes`) → Modal mit „Keine Klassen vorhanden." statt eines leeren Dialogs. Nach der Wahl lädt `ladeLehrerwand()` per AJAX `cbd_fragenwand_get_notes` und ruft `open(classId, notes, {verwaltbar: true})`. **Verwaltungsmodus:** Haken klickbar (`change`, nicht `click` — im `<label>` liefe `click` doppelt) → `cbd_fragenwand_toggle_note`; je Notiz „Bearbeiten" (Inline-Feld im `<li>`, Enter speichert, Escape bricht ab und wird per `stopPropagation()` von `beiTaste` ferngehalten, sonst schlösse es das ganze Modal) → `cbd_fragenwand_edit_note` und „Löschen" → `cbd_fragenwand_delete_note`; unter der Liste `div.cbd-fragenwand-neu` mit Feld + Knopf → `cbd_fragenwand_add_note`. **Nach jeder Schreibaktion wird die Liste neu vom Server geholt** (`ladeListeNeu()`, ersetzt nur den Körper, nicht das Overlay) statt das DOM fortzuschreiben: Die Reihenfolge steht im `ORDER BY` von AP-2.2, ein Haken verschiebt die Notiz ans Ende. Alle Lehrer-Aufrufe gehen über `rufeAjax()` mit `ajaxUrl`/`nonce` aus `cbdFragenwandFrontend` (seit AP-3.fix1; `window.cbdClassroomData` dient nur noch als Rückfall, falls eine Seite aus einem Cache von vor diesem Stand stammt — Feldname `nonce`, wie `check_ajax_referer('cbd_classroom_nonce', 'nonce')` erwartet); eine fehlgeschlagene Nonce-Prüfung liefert `-1` statt JSON und landet daher im `catch`-Zweig. Ein Modulflag `schreibtGerade` verhindert überlappende Schreibanfragen, `fokusWunsch` stellt den Tastaturfokus nach dem Neuaufbau wieder her. Leere Texte werden **nicht** im Browser abgefangen, sondern vom Server abgelehnt — die Regel soll nicht in zwei Fassungen existieren. **Schüler-/Besuchermodus:** `GET cbd/v1/fragenwand` mit `restUrl` aus `wp_localize_script` (`cbdFragenwandFrontend`, NIE im JS zusammengesetzt — `?rest_route=`-Installationen) plus `window.location.search` **unverändert** angehängt (`?` bzw. `&` je nach Basis, Vorbild `mitParameter()` in `blocks/block-reference/view.js`); welche Parameter die Sitzung ausmachen, entscheidet allein `CBD_Classroom_Gate::sitzung()` serverseitig. HTTP 200 → `open(null, notes)`, HTTP 404 → Modal mit „Keine aktive Klassensitzung." (zeichengleich für jede Ursache, wie der Endpunkt selbst). Ein `abrufZaehler` verwirft Antworten, die nach dem Schließen oder einem zweiten Klick eintreffen. **Markup-Vertrag mit AP-3.4** (Stand nach AP-3.3, damit final): `.cbd-fragenwand-overlay` > `.cbd-fragenwand-dialog[role=dialog][data-modus=lesen\|verwalten\|meldung]` > `.cbd-fragenwand-kopf` (`-titel`, `-schliessen`) + `.cbd-fragenwand-koerper` + `p.cbd-fragenwand-status[role=status][hidden]` (Fehler einzelner Schreibaktionen; liegt **neben** dem Körper, weil `leere(koerper)` sie sonst bei jedem Neuaufbau mitlöschte). Im Körper `ul.cbd-fragenwand-liste` > `li.cbd-fragenwand-notiz[data-note-id]` (erledigt zusätzlich `.cbd-fragenwand-notiz--erledigt`) mit `label.cbd-fragenwand-notiz__label` (`__haken`, `__text`) und — nur im Verwaltungsmodus — `div.cbd-fragenwand-notiz__aktionen` (`__bearbeiten`, `__loeschen`), beim Bearbeiten zusätzlich `div.cbd-fragenwand-notiz__bearbeitung` (`__feld`, `__speichern`, `__abbrechen`); bzw. `p.cbd-fragenwand-meldung`. Im Verwaltungsmodus folgt `div.cbd-fragenwand-neu` (`__feld`, `__knopf`). Die Klassenauswahl liegt separat unter `.cbd-fragenwand-klassenwahl-overlay` > `-dialog` (`-titel`, `-hinweis`, `-optionen` > `-option` > `-name`, `-aktionen` > `-abbrechen`). Verwaltbar ist die Wand nur bei `optionen.verwaltbar === true` **und** brauchbarer `classId` — sonst (insbesondere `classId === null`, dem Schülerfall) Checkboxen `disabled` (**nicht** `readonly`, das wirkt auf Checkboxen im HTML-Standard gar nicht). Notiztexte ausschließlich per `textContent`. Schließen über Backdrop-Klick (nur wenn `e.target === overlay`), Schließen-Knopf und Escape; Fokus geht beim Öffnen auf den Schließen-Knopf, beim Schließen zurück zum Auslöser, dazwischen hält eine Fokusfalle Tab im Dialog (Muster aus `blocks/block-reference/view.js`). **Bringt bewusst keine Inline-Styles und keine CSS-Datei mit** — die Optik kommt in AP-3.4. Eingereiht von `CBD_Fragenwand::enqueue_frontend_assets()` auf `wp_enqueue_scripts`, **unbedingt auf jeder Frontend-Seite**. **Seit dem Hotfix „Fragenwand in Klassenlisten" (2026-08-29)** nimmt `ladeSchuelerwand(trigger)` den angeklickten Auslöser entgegen und reicht ihn an `baueAbrufUrl(trigger)` weiter: Der neue Helfer `sitzungAusTrigger()` liest `data-classroom` **und** `data-token` vom Auslöser und bildet daraus die Abfragezeichenfolge; nur wenn eines der beiden Attribute fehlt, gilt weiterhin `window.location.search` unverändert. Nötig für die Seitenliste der `[cbd_classroom]`-Seite, die nach dem reinen AJAX-Login **keine** Sitzungsparameter in der Adresse hat (Details: `CLAUDE.md`, Abschnitt „Fragenwand", Unterabschnitt „Hotfix: derselbe Knopf in den zwei JS-gebauten Klassenlisten"). Abwärtskompatibel: Verweise ohne die Attribute verhalten sich exakt wie zuvor, und der Lehrer-Weg ist unberührt | `class-cbd-fragenwand.php`, `assets/js/fragenwand-format.js`, (ab AP-3.4) `assets/css/fragenwand.css`, (ab dem Hotfix) `assets/js/classroom-frontend.js`, `assets/js/classroom-page-filter.js`. **Vorhaben „Schüler-Fragen" (3.1.113):** `baueNotiz()` setzt bei `ist_schueler_frage` zusätzlich `cbd-fragenwand-notiz--schueler` und hängt die Beschriftung `__herkunft` ein — dieselbe Funktion bedient BEIDE Renderpfade, die Kennzeichnung erscheint dadurch in Lehrer- wie Schülersicht. Neu: `baueSchuelerNeuBereich()` (Feld „Frage stellen" samt Anonymitätshinweis, nur im Lesemodus und nur wenn `schuelerDarfFragen`) und `sendeSchuelerFrage()` (POST auf dieselbe REST-Route, Adresse über `baueAbrufUrl(schuelerAusloeser)` — damit funktionieren `?classroom=&token=` UND der Datenattribut-Weg) |
| `klassenpuls.js` | **Taktgeber des Klassenmodus** (Vorhaben „Klassenmodus-Live" AP-1.4) — ein einziger Taktgeber je Tab fragt `cbd/v1/klassenpuls` regelmäßig ab und benachrichtigt Abonnenten, wenn sich eine Signatur ändert; ab Phase 2 hängen sich `classroom-page-filter.js`, `classroom-frontend.js` und `fragenwand-frontend.js` daran, jeweils ohne voneinander zu wissen — so hängt die Serverlast an der Schülerzahl, nicht an der Zahl der Live-Funktionen. **Öffentliche Schnittstelle `window.cbdKlassenpuls`**, sieben Methoden: `setzeSitzung(classroomId, token)`, `setzeSeite(pageId)`, `abonniere(name, rueckruf)` (gibt Abmeldefunktion zurück), `starte()`, `halte()`, `sofort()` (eine Abfrage jetzt), `laeuft()` (bool). `setzeSitzung()` fasst eine bereits über `setzeSeite()` gesetzte `page_id` **nicht an** — wichtig, weil ab AP-4.2 sowohl der Seitenfilter als auch die Fragenwand `setzeSitzung()` auf derselben Seite aufrufen können. `name` in `abonniere()` ist einer der vier Signaturnamen der Serverantwort — `seite`, `tafel`, `klasse`, `fragenwand` — oder der künstliche fünfte Name `abgelaufen` (Sitzung ungültig, Server antwortet HTTP 404: Taktgeber stellt sich **endgültig** ein und ruft `abgelaufen`-Abonnenten genau einmal auf, kein Weiterprobieren). **Kernregel: Die erste Serverantwort löst keinen Rückruf aus** — sie legt nur die Ausgangssignaturen fest (`erstanfrageErledigt`); erst eine davon **abweichende** Folgeantwort benachrichtigt Abonnenten, sonst würde direkt nach dem Seitenaufbau alles unnötig neu geladen. Rückzug bei Netzfehlern (ab dem dritten Fehlschlag in Folge Intervall verdoppeln, gedeckelt bei 120 s, nach Erfolg zurück auf den Servertakt), Pause bei `document.hidden` (sofortige Abfrage beim Zurückwechseln), Takt kommt ausschließlich aus dem Feld `takt` der Serverantwort (`takt: 0` hält endgültig an — die Notbremse `cbd_klassenpuls_takt`). **Seit AP-1.fix1 (Befund aus `docs/messung-klassenpuls.md`, Abschnitt 5) streut `aktuellesIntervallMs()` das Intervall im Normalbetrieb bei JEDER Planung neu um ±25 % um den Takt** (`basis * (0,75 + Math.random() * 0,5)`, danach auf mindestens 5000 ms geklemmt) — ohne Streuung blieben gemeinsam gestartete Abfrageschleifen (gemeinsamer Stundenbeginn einer Klasse) über zehn Minuten in einem 0,5–0,65 s breiten Zeitband, statt sich aufzulösen. Die Streuung greift **ausschließlich** im Normalbetrieb (`fehlerZaehler < RUECKZUG_AB_FEHLER`) und wird **nicht** zusätzlich auf die bereits verdoppelnden Rückzugsstufen aufaddiert. Ohne `cbdKlassenpulsDaten.restUrl` (aus `wp_localize_script()`, AP-1.5) und ohne einen `setzeSitzung()`-Aufruf findet **keine** Netzwerkanfrage statt. Reines ES5 in einer IIFE, kein Build-Schritt, kein jQuery (`fetch()` genügt), kein `import`/`export`. In Phase 1 noch **ohne** Abonnenten — wird erst in AP-1.5 eingereiht und lokalisiert | `class-cbd-klassenpuls.php` (liefert die Route), (ab AP-1.5) `class-cbd-classroom.php` (Enqueue + `wp_localize_script()`) |
| `block-recovery.js` | Repariert ungültige Blöcke beim Öffnen des Editors — Sicherheitsnetz für Markup-Abweichungen |
| `interactivity-store.js` / `interactivity-fallback.js` | Frontend-Verhalten (Aufklappen, Kopieren, Screenshot); Interactivity API mit jQuery-Rückfall. **`tryWebShare()` ist ein `function*` und wird mit `yield*` delegiert** (seit 3.1.88) — mit einfachem `yield` lief der Rumpf nie, Web Share und Download entfielen, und der Screenshot meldete Erfolg ohne Datei. Canvas-Fläche pro Gerät gedeckelt, `backgroundColor: '#ffffff'`, Fehlschlag zeigt 3 s Warn-Icon. **Bricht der Nutzer den nativen Teilen-Dialog ab (`AbortError`), wird das Icon stillschweigend zurückgestellt** — kein Warn-Icon, kein `console.error`; in `interactivity-fallback.js` leistet das bereits `resetButton()`. **Apple-Weiche (AP-2.6):** `istAppleGeraet()` erkennt iOS/iPadOS (`/iPad|iPhone|iPod/` bzw. `MacIntel` mit `maxTouchPoints > 1`) **und macOS-Safari** (`navigator.vendor` enthält `Apple`, UA enthält `Safari`, aber weder `Chrome`, `Chromium`, `Edg` noch `OPR`) — die ältere Erkennung in `pdf-server-side.js` deckt macOS-Safari **nicht** ab. Dort wird der Screenshot-Knopf beim Initialisieren zum PDF-Knopf (`data-cbd-apple-pdf="1"`), und `createScreenshot` leitet **vor** jeder html2canvas-Berührung um auf `window.cbdPDFExportServerSide([$(mainContainer)], 'visual')` — derselbe Aufruf steht in `*createPDF()`. Fehlt die Funktion, wird der Knopf versteckt. Der Knopf bleibt an das Feature-Flag `screenshot` gebunden. **`class-cbd-block-registration.php` wird dafür bewusst nicht angefasst** — eine Apple-Erkennung im gerenderten HTML vergiftete jeden Full-Page-Cache |
| `block-numbering.js` | Nummerierung im Browser (WordPress rendert Blöcke nicht in Dokumentreihenfolge) |
| `latex-renderer.js` | KaTeX-Anbindung; stellt **`window.cbdRenderLatex(root)`** bereit → `Promise<number>`, löst erst nach `document.fonts.ready` auf (danach ist Höhenmessung verlässlich), rendert nur Formeln ohne `data-cbd-latex-rendered="1"`, gescheiterte tragen `data-cbd-latex-failed="1"`. Liefert `0` statt zu werfen, wenn KaTeX fehlt. **Fremde Plugins müssen `typeof window.cbdRenderLatex === 'function'` prüfen.** MutationObserver startet beim Laden der Datei, dazu ein entprellter `resize`-Listener (150 ms). Aufrufer: `blocks/accordion/view.js` in „Eigene WP Blocks" |
| `floating-pdf-button.js` | PDF-Knopf und Werkzeugleiste; **stylt inline per jQuery**, nicht über CSS. **Seit AP-2.9 (`PLAN-CSS-Variablen-Darkmode.md`):** Status-/Grundfarben (`--color-success`/`--color-danger`/`--color-background`/`--color-text-primary`) per `getComputedStyle` gelesen, gleiches Muster wie die schon zuvor gelesene `--color-ui-surface`; Plastik-Look (`color-mix()`) sowie reine Overlay-/Glanz-`rgba()` (Schwarz- und Weißtöne ohne passende Variable) bleiben bewusst Literal. **Seit AP-2.4 (`PLAN-Darkmode-Umschaltung.md`, Phase 2, Sichtprüfung):** echter Kontrastfehler gefunden und behoben — die weiße Text-/Icon-Farbe auf der orangen Plastikfläche (FAB, Werkzeugleiste, Badges, die bewusst flache `.cbd-pdf-go`-Schaltfläche) wurde aus `--color-background` gelesen, die im Darkmode dunkel (`#121212`) wird, während die orange Fläche selbst unverändert bleibt — Kontrast fiel auf ≈3:1 (unter WCAG AA), `.cbd-pdf-go` wurde fälschlich dunkel statt weiß. Fix: neue lokale Variable `colorOnAccent` liest stattdessen `--color-text-on-accent` (in beiden Modi `#ffffff`), nur an den Text-auf-Akzentfläche-Stellen; die `select option`-Farbpaarung bleibt bewusst bei `colorBackground`/`colorTextPrimary`. Lightmode dabei unverändert, echter PDF-Export weiterhin funktionsfähig getestet. **Seit AP-2.4 aus `PLAN-PDF-Notizen-und-Listenformeln.md` (2026-08-24, nicht zu verwechseln mit dem gleichnamigen AP-2.4 aus `PLAN-Darkmode-Umschaltung.md` oben):** Neue Checkbox `.cbd-pdf-drawings-check` (Label `.cbd-pdf-drawings-toggle`) in der Export-Toolbar, Default angehakt, liest `startPDFExport()` aus und reicht sie als 4. Parameter `includeDrawings` an `window.cbdPDFExportServerSide()` durch (Steuerung, ob Tafelbilder/eigene Notizen mit exportiert werden — Auswertung in `pdf-server-side.js`, siehe dort) |
| `pdf-server-side.js` / `html2pdf-loader.js` | PDF-Erzeugung. **Seit AP-2.3 aus `PLAN-PDF-Notizen-und-Listenformeln.md` (2026-08-24):** `window.cbdPDFExportServerSide(containerBlocks, mode, quality, includeDrawings)` bekam einen 4. Parameter `includeDrawings` (Default `true` bei `undefined`/fehlend — bestehende Aufrufer wie die Apple-PDF-Weiche in `interactivity-store.js` bleiben unverändert), durchgereicht bis `processOneBlock()` und dort um den bestehenden Aufruf von `injectDrawingsFromStorage()` (lokale „Eigene Notizen") gelegt. Neue Funktionen `injectServerDrawings()`/`applyServerDrawings()`: lesen je Container den Begleitschlüssel `localStorage['cbd-board-' + stableId + '-classid']` (aus `board-mode.js`, AP-2.2) und laden darüber serverseitige Tafelbilder über den Bulk-Endpoint `cbd_get_page_drawings` (AP-2.1, `class-cbd-classroom.php`) nach — **ein AJAX-Aufruf je eindeutiger `class_id`**, nicht je Container (exportlaufweiter Cache `serverDrawingsCache`, nötig weil `processOneBlock()` pro ausgewähltem Container einzeln läuft). Label im PDF „Tafelbild" statt „Eigene Notiz" zur Unterscheidung. **Zusatzfund:** `cbdPDFData` wird an zwei unabhängigen Stellen lokalisiert (`class-cbd-classroom.php`, nur auf Seiten mit `[cbd_classroom]`-Shortcode, **und** `class-cbd-block-registration.php` im Normalfall ohne Klassen-Shortcode, dort fehlen `pageId`/`classroomNonce`) — dagegen ein Client-Fallback auf `window.cbdClassroomData || cbdPDFData` eingebaut. **Bekannte, bewusst akzeptierte Einschränkungen:** Der `-classid`-Begleitschlüssel deckt nur Seite 0 ab (kein `:pN`-Suffix für Zusatzseiten mehrseitiger Tafelbilder, AP-2.2); ein Container mit Tafelbildern für mehrere Klassen exportiert nur die zuletzt genutzte Klassen-Zuordnung. Details und Datenweg: `CLAUDE.md`, Abschnitt „PDF-Export: Tafelbilder und eigene Notizen". **Seit AP-2.9:** Fortschritts-Overlay (`pdf-server-side.js`) bzw. sichtbarer Wrapper-/Lade-Hintergrund (`html2pdf-loader.js`) ebenfalls per `getComputedStyle` an Theme-Variablen gekoppelt; PDF-Inhaltsfarben (Server-HTML für mPDF, html2canvas-Renderhintergrund) bewusst unangetastet. **Behoben durch `PLAN-PDF-Export-und-Tafelmodus-Fixes.md` (AP-1.2, 2026-08-25):** Der vormals hier geführte Fund „`collectCSSVariables()` liest zwei falsch benannte CSS-Variablen" ist korrigiert (jetzt `--color-text-primary`/`--color-background-light`, greift seither korrekt). Dieselbe Funktion entfernt seit **AP-1.fix1** kurzzeitig `data-theme="dark"` vom `<html>`-Element vor dem synchronen `getComputedStyle()`-Aufruf und stellt es direkt danach wieder her — ein PDF-Export bildet den Website-Darkmode dadurch grundsätzlich **nicht** ab, unabhängig vom beim Export angezeigten Website-Modus (kein sichtbares Flackern, da zwischen Entfernen/Wiederherstellen kein Repaint liegt). **AP-1.2 behebt zusätzlich zwei weitere Ursachen für fehlende Notizen/Tafelbilder im PDF:** `recompressBase64()` bekam einen 4. Parameter `outputFormat` (Default `'image/jpeg'`); beide Zeichnungs-Aufrufstellen (`injectDrawingsFromStorage()` und der Server-Tafelbild-Pfad) übergeben jetzt `'image/png'` statt der vorherigen verlustbehafteten JPEG-Rekompression, die Transparenz in Notizen-PNGs zu einem schwarzen Rechteck zerstörte. `processOneBlock()`s Aufräum-Selektoren fehlte `.cbd-pdf-drawing-section` (Namensdiskrepanz zu den tatsächlich eingefügten Wrappern) — ergänzt; erwies sich bei der AP-1.rev-Prüfung als folgenlos, da `$block.clone()` ohnehin nie das Original-DOM verändert und Duplikate strukturell ausgeschlossen waren. **`html2pdf-loader.js`:** behauptet in seinem Kopfkommentar „no longer enqueued", ist aber über `class-cbd-classroom.php` weiterhin als Abhängigkeit von `floating-pdf-button.js` enqueued und lädt im Fehlerfall von drei CDN-Quellen (cdnjs/unpkg/jsdelivr) — DSGVO-Altlast, unabhängig von diesem Vorhaben |
| `icon-picker.js` | Icon-Auswahl im Admin |
| `board-mode.js` | Tafelmodus. **Seit AP-2.2 aus `PLAN-PDF-Notizen-und-Listenformeln.md` (2026-08-24):** `saveToServer()`/`loadFromServer()` schreiben im Erfolgszweig zusätzlich einen Begleitschlüssel `localStorage['cbd-board-' + containerId + '-classid'] = classId` (analog zum bestehenden `-bgcolor`-Schlüssel) — damit weiß der PDF-Export (`pdf-server-side.js`), welcher Klasse ein Container zuletzt zugeordnet war. **Werte werden synchron VOR dem `fetch`-Aufruf erfasst, nicht erst im `.then()`** — `close()` setzt `this.classId` synchron auf `null`, bevor eine laufende Anfrage aufgelöst ist; ein Lesen im `.then()` schriebe reproduzierbar `"null"`. Deckt nur Seite 0 ab (`<stableId>`, kein `:pN`-Suffix für Zusatzseiten eines mehrseitigen Tafelbilds). **Seit AP-1.5 aus `PLAN-PDF-Export-und-Tafelmodus-Fixes.md` (2026-08-25):** neue Methode `updateDarkModeInversion()` setzt/entfernt die Klasse `cbd-board-inverted` auf `.cbd-board-canvas-container`, wenn `data-theme="dark"` UND `this.boardColor === '#ffffff'` — reine Anzeige-Invertierung per CSS-Filter, die gespeicherten Pixel bleiben unverändert. Aufgerufen aus `setBoardColor()` (deckt Board-Öffnen via `init()` und jeden Farbwechsel über den Tafelfarbe-Zyklus-Button ab). **Bekannte, akzeptierte Lücke:** Der Darkmode-Toggle des Themes (`Theme/header.php`) setzt `data-theme` ohne Seiten-Reload, aber ohne eigenen Listener hier — wird der Toggle bei bereits offenem Tafelmodus geklickt, greift die Invertierung erst beim nächsten Board-Öffnen/Farbwechsel, nicht live. Bewusst nicht implementiert (Scope-Grenze laut Plan). |
| `classroom-admin.js` / `classroom-frontend.js` / `classroom-page-filter.js` | Klassenverwaltung. **`classroom-frontend.js` seit AP-1.2 aus `PLAN-Inhaltsverzeichnisse.md` (2026-08-26):** `renderClassroomContent()` baut die Seitenliste nach dem Klasseneinstieg nicht mehr flach (ein `<div>` je Seite, Ebene nur als inline `margin-left: level*24px`), sondern als echte, klappbare Baumstruktur — levelbasierter Stack aus `<ul>`-Containern, adaptiert von `injectClassroomSidebar()` in `classroom-page-filter.js`. Markup: `<ul class="cbd-classroom-page-tree">` → `<li class="cbd-classroom-page-item cbd-level-N">` mit `<div class="cbd-classroom-page-row">` (Zeile) und, sofern Kinder vorhanden, `<ul class="cbd-classroom-children">`; Knoten mit Kindern bekommen `cbd-classroom-has-children cbd-classroom-expanded` und einen Knopf `.cbd-classroom-toggle`. **Standardzustand aufgeklappt** (keine Verhaltensänderung; die Speicherung des Klappzustands folgt in AP-1.3a). **Das ID-Feld der AJAX-Antwort `cbd_student_get_data` heißt `page.page_id`, NICHT `page.id`** — am echten Netzwerk-Response verifiziert (Fund aus AP-1.3b); es landet als `data-page-id` am `<li>`. Der Toggle-Klick läuft über Event-Delegation am Container `#cbd-classroom-pages` im Namensraum `click.cbdClassroomToggle`, der vor dem Binden abgemeldet wird — der Container überlebt das `.empty()` beim Neuaufbau, sonst hingen nach einem zweiten Rendern (Auto-Login, Token-Erneuerung) zwei Handler am selben Knopf. **Seit AP-1.3a (2026-08-26):** `renderClassroomContent()` liest vor dem Baumaufbau `cbdKlassenverzeichnisGeleseneCollapsedIds()` (private Hilfsfunktion in der Datei-IIFE, `try/catch` um `localStorage.getItem()`/`JSON.parse()`) und baut einen Knoten ohne `cbd-classroom-expanded` sowie mit `aria-expanded="false"` auf, wenn seine `page_id` (als String) in der gelesenen Liste steht; der Toggle-Klick-Handler schreibt die aktualisierte Liste über `cbdKlassenverzeichnisSchreibeCollapsedIds()` zurück. Schlüssel `cbd_classroom_toc_collapsed`, JSON-Array zugeklappter Seiten-IDs als Strings — Vertrag dokumentiert in `CLAUDE.md`, Abschnitt „Klassenmodus: Klappbare Inhaltsverzeichnisse". **`classroom-page-filter.js` seit AP-1.3b (2026-08-26):** `injectClassroomSidebar()` legt beim Aufbau jedes `<li class="page-item">` zusätzlich `data-page-id="<page.page_id>"` ab (**nicht** `page.id` — derselbe Datenfeld-Fund wie oben) und nutzt eine eigene, wortgleiche Kopie derselben zwei Hilfsfunktionen gegen denselben Schlüssel `cbd_classroom_toc_collapsed`, damit ein in der Login-Liste zugeklapptes Kapitel auch in der Klassen-Seitenleiste zugeklappt erscheint (und umgekehrt). **Bekannte Einschränkung (Review-Befund B3):** Anders als in `classroom-frontend.js` bekommt ein zugeklappt startender Knoten hier **kein** explizites `aria-expanded="false"` beim Aufbau (bleibt zunächst `null`, erst der erste Klick setzt einen Wert) — kein funktionaler Fehler, aber eine Inkonsistenz zwischen beiden Ansichten, siehe `CLAUDE.md`. **Beide Dateien seit dem Hotfix „Fragenwand in Klassenlisten" (2026-08-29):** Jede setzt ganz oben in ihre Liste einen eigenen Fragenwand-Knopf (`button.cbd-fragenwand-verweis.page-index__fragenwand-link` in `div.cbd-classroom-fragenwand.page-index__zusatz.page-index__zusatz--fragenwand`) — der Theme-Filter `simple_clean_page_index_zusatzeintraege` (AP-4.2) erreicht diese rein im Browser gebauten Listen nicht. Keine eigene Klick-Logik: Der delegierte Listener in `fragenwand-frontend.js` hängt an `document`. **Unterschied zwischen beiden:** `classroom-frontend.js` schreibt zusätzlich `data-classroom`/`data-token` (aus `this.classId`/`this.token`) auf den Knopf, weil die `[cbd_classroom]`-Seite nach dem AJAX-Login kein `?classroom=&token=` in der Adresse hat; `classroom-page-filter.js` verzichtet bewusst darauf, da `init()` dort ohne beide URL-Parameter gar nicht erst startet. Details: `CLAUDE.md`, Abschnitt „Fragenwand", Unterabschnitt „Hotfix: derselbe Knopf in den zwei JS-gebauten Klassenlisten". **Vorhaben „Schüler-Fragen" (3.1.113):** `classroom-admin.js` schickt in `saveClass()` immer `schueler_fragen_erlaubt` als `'1'`/`'0'` mit (nie weglassen — eine abgehakte Checkbox sendet von sich aus nichts, das Ausschalten käme sonst nie an), `editClass()` und `resetForm()` setzen die Checkbox `#cbd-class-schueler-fragen` |
| `lehrer-klassen.js` | **Seitenauswahl der Frontend-Klassenverwaltung** (`[cbd_lehrer_klassen]`, seit 3.1.115) — einzige Aufgabe: die `<option>`-Elemente der Seitenzuordnung aus `window.cbdLehrerKlassen.seiten` nachtragen. **Ohne jQuery**, läuft synchron beim Parsen im Footer. **Warum die Liste nicht einfach im Markup steht:** Der Shortcode-Rückgabewert läuft durch `the_content` und damit durch die Glossar-Autoverlinkung des Themes (Priorität 10000), die den Inhalt in Textstücke zerlegt und auf jedes ein aus Glossarbegriffen gebautes Muster anwendet — rund 280 Seitentitel wären 280 zusätzliche Textstücke. `CBD_Classroom::lehrer_klassen_seitendaten()` liefert die Liste deshalb als Array, das `wp_localize_script()` an `the_content` vorbei in den Footer schreibt. **Reihenfolge ist Vertrag:** `cbd-classroom-admin` ist von `cbd-lehrer-klassen` abhängig, denn `init()` in `classroom-admin.js` klont `.cbd-page-selector` als Vorlage und braucht die Optionen bereits. Titel wird über `textContent` gesetzt (kann kein Markup einschleusen); `data-parent` ist Vertrag mit `getChildPages()` in `classroom-admin.js`. Details: `CLAUDE.md`, Abschnitt „Klassenverwaltung im Frontend" |
| `personal-notes-manager.js` | Persönliche Notizen |
| `admin.js`, `admin-common.js`, `admin-blocks-list.js`, `admin-import-export.js`, `admin-live-preview-fix.js` | Admin-Oberfläche |

## Gestaltung (`assets/css/`)

| Datei | Zweck |
|---|---|
| `cbd-frontend-clean.css` | **Die lebende Frontend-Datei.** Kopfzeilen-Icon, Icon-Größen je Bibliothek, `--cbd-icon-scale` als Rückfallwert. **Icon-Position (AP-2.2 aus `PLAN-CSS-Variablen-Darkmode.md`):** Alle **drei** `transform`-Regeln von `.cbd-header-icon` (Desktop −6px, ≤768px −4px, ≤480px −3px) rechnen `--cbd-icon-dx`/`--cbd-icon-dy` in ihren **je eigenen** Basiswert ein — ein serverseitig gesetztes `transform` löschte alle drei und bräche die Handy-Ausrichtung. Eckmodus: `.cbd-icon-positioned` (`position:absolute; z-index:3; margin-right:0`) plus `.cbd-icon-at-<ecke>` mit 10px Grundabstand. **Bekannte geometrische Kollision, unbehoben:** `.cbd-action-buttons` sitzt auf demselben Rahmen ebenfalls auf `top:10px; right:10px` mit `z-index:9999` gegen `z-index:3` — ein Icon in `container-top-right` mit Versatz 0/0 wird beim Überfahren des Containers von der Knopfleiste verdeckt. Abhilfe ist ein Feinversatz. **Seit AP-2.1 (Phase 2 CSS-Variablen, 3.1.100):** freistehende Hex-Werte im damaligen `@media (prefers-color-scheme: dark)`-Block, in den übrigen Icon-/Aktionsleisten-Regeln und im Behandelt-Dialog auf `var(--x, #bisheriger-wert)` umgestellt — der Fallback ist überall der bisherige Wert, optisch pixelgleich. Bootstrap-/Material-Design-Farben ohne exakte Vokabular-Entsprechung blieben mit Begründungskommentar literal. **Bekannter, vorbestehender Mismatch (nicht durch AP-2.1 eingeführt):** drei `.cbd-menu-toggle`/`.cbd-dropdown-menu`-Regeln nutzen `var(--color-sidebar-border, #ddd)`, obwohl die Variable live `#e0e0e0` ist — der Fallback greift nie (Variable ist definiert), folgenlos, für ein künftiges Aufräum-AP vorgemerkt. **Seit AP-2.2 (`PLAN-Darkmode-Umschaltung.md`, Phase 2, 3.1.10x):** Der `@media (prefers-color-scheme: dark)`-Block für `.cbd-container-block` wurde auf `[data-theme="dark"] .cbd-container-block` umgestellt (Mechanismus siehe `Theme/CLAUDE.md`, Abschnitt „Darkmode") — Systemvoreinstellung wird seither ignoriert, nur der Toggle zählt. Der Live-Test deckte dabei **drei Kaskade-Bugs** auf, die den mechanischen Umbau allein wirkungslos gemacht hätten, alle behoben: (1) das gerenderte Inline-`style`-Attribut jedes echten Containers (Hintergrund/Rahmen/Textfarbe aus der DB) schlägt eine Nicht-`!important`-Regel immer — Fix: `!important` auf `background`/`border-color`/`color`; (2) eine dauerhaft aktive, sich neu triggernde `transition: all 0.3s ease` hat nach CSS Cascade Level 4 Vorrang selbst vor Author-`!important` — Fix: neue, modusunabhängige Regel `.cbd-container-block { transition-property: box-shadow, transform !important; }`, die Hintergrund/Rahmen/Textfarbe grundsätzlich von jeder Transition ausschließt; (3) **Variablen-Kollision:** `.cbd-container-block`, `.cbd-collapse-toggle` und `.cbd-icon .dashicons` nutzten `color: var(--color-sidebar-border, #e0e0e0)` rein wertbasiert — seit `PLAN-CSS-Variablen-Darkmode.md` AP-1.1 löst diese Variable im Darkmode zu `#444444` (ihre Rolle als Rahmenfarbe) auf, Kontrast fiel auf ≈1,3–1,6:1; umgestellt auf die semantisch passende `color: var(--color-text-primary, #e0e0e0)` (Fallback unverändert), Kontrast danach `#e8e8e8` auf `#1e1e1e`. Fund (1) und (2) sind vermutlich nicht darkmode-spezifisch, sondern bestanden schon vorher latent |
| `custom-icons.css` | Eigene SVG-Kacheln und Nummern-Kacheln; hängt per Handle an `cbd-frontend-clean` |
| `block-base.css` | Grundlegende Block-Regeln, vom Style-Loader eingebunden. **Seit AP-2.7 (Phase 2, 3.1.100):** ein echter Treffer auf `var(--color-text-primary, #333)` umgestellt (`.cbd-feature-button`); vier weitere Literale (`#ccc`, `#f9f9f9`, `#e9e9e9`, `#999`) ohne Vokabular-Entsprechung bewusst belassen. **Nebenbefund:** `.cbd-feature-button`/`.cbd-block-icon`/`.cbd-block-number` scheinen im aktuellen Rendering-Pfad unbenutzt (0 Treffer im DOM der Testseite) — möglicherweise historischer, abgelöster Code |
| `block-responsive.css` | Responsive Block-Regeln, vom Style-Loader eingebunden. **AP-2.7 geprüft (Phase 2, 3.1.100):** 0 Hex-Werte enthalten, keine Änderung nötig |
| `editor-base.css` | vom Style-Loader eingebunden |
| `frontend-consolidated.css` | vom Style-Loader eingebunden. **Editor-only, NICHT im Zuge von `PLAN-Darkmode-Umschaltung.md` umgestellt (AP-2.1, Phase 2):** Der Handle `cbd-editor-frontend-consolidated` wird ausschließlich in `class-cbd-style-loader.php::enqueue_editor_styles()` registriert, einem reinen `enqueue_block_editor_assets`-Hook, der beim Rendern einer Frontend-Seite nie feuert; empirisch bestätigt (0 Treffer im Seitenquelltext einer echten Frontend-Seite mit Container-Block). Der noch vorhandene `@media (prefers-color-scheme: dark)`-Block in dieser Datei bleibt deshalb bewusst bestehen — Admin-/Editor-Oberflächen sind laut Plan-Nicht-Ziel außerhalb des Scopes |
| `block-editor.css` | Editor-Gestaltung (die Frontend-Skalierung erreicht ihn **nicht**) |
| `content-importer.css` | Import-Dialog im Editor |
| `page-importer.css` | Importseite (seit 3.1.86) |
| `latex-formulas.css` | Gestaltung gerenderter LaTeX-Formeln. **Seit AP-2.4 aus `PLAN-CSS-Variablen-Darkmode.md` (Phase 2, 3.1.100):** zwei echte Kopplungen an Theme-Variablen (`color: var(--color-text-primary, #333)`; im damaligen `@media (prefers-color-scheme: dark)`-Block zusätzlich, rein wertbasiert, `var(--color-sidebar-border, #e0e0e0)`). KaTeX-Fehlerfarbe `#cc0000`, Fehlerhintergrund `#ffe6e6`, Times-Serif-Fallback, Dark-Mode-spezifische Literale und WP-Admin-Blau im Fokus-Outline blieben mit Begründungskommentar literal — kein Wert entsprach exakt einer Theme-Variablen. `assets/js/latex-renderer.js` wurde im selben AP mitgeprüft und blieb funktional unverändert; einzige Ergänzung ist ein Begründungskommentar an `errorColor: '#cc0000'` (KaTeX-API-Option, kein DOM-Inline-Style, wertlich ≠ `--color-danger` `#cc3333`). **Seit AP-2.3 (`PLAN-Darkmode-Umschaltung.md`, Phase 2, 3.1.10x):** Der `@media (prefers-color-scheme: dark)`-Block für `.cbd-latex-fallback`/`.cbd-latex-error` wurde in zwei eigenständige `[data-theme="dark"] .cbd-latex-fallback`/`[data-theme="dark"] .cbd-latex-error`-Regeln umgewandelt (Mechanismus siehe `Theme/CLAUDE.md`, Abschnitt „Darkmode"); vorhandene Werteherkunfts-Kommentare blieben erhalten. Der Live-Test (per DOM-Injektion, da keine reale Seite eine fehlerhafte Formel zeigt) deckte dieselbe Variablen-Kollision wie bei `cbd-frontend-clean.css` auf: `.cbd-latex-fallback` nutzte `color: var(--color-sidebar-border, #e0e0e0)`, löst im Darkmode zu `#444444` auf (Kontrast ≈1,3:1 auf `#2a2a2a`) — umgestellt auf `color: var(--color-text-primary, #e0e0e0)` (Fallback unverändert), danach `#e8e8e8` auf `#2a2a2a`. `.cbd-latex-error` unverändert (nutzt bereits literales `#ff6b6b`, keine Variable, kein Kollisionsrisiko) |
| `interactivity-api.css` | Kern-Interaktivität der Container-Blöcke (Aufklappen, Kopieren, Screenshot). **AP-2.5 geprüft (Phase 2, 3.1.100):** kein Hex-Wert der Datei traf eine Theme-Variable exakt — Dashicon-Fehlerfarbe `#dc3232` (≠ `--color-danger` `#cc3333`) und Debug-`font-family: monospace` (≠ exakter `--font-family-mono`-Stack) blieben mit Begründungskommentar literal, keine tatsächliche `var()`-Umstellung. `interactivity-store.js`/`interactivity-fallback.js` ebenso geprüft und unverändert — der Screenshot-Canvas-Hintergrund (`backgroundColor: '#ffffff'`) bleibt bewusst Weiß statt ans Theme gekoppelt, sonst wäre ein exportierter Screenshot bei künftigem Darkmode unlesbar. **Bekannter, vorbestehender Mismatch (nicht durch AP-2.5 eingeführt):** Zeile 60 nutzt bereits `var(--color-ui-surface, #46b450)` — der grüne Fallback passt weder wertlich noch semantisch zur tatsächlich orangen Variable (`#e24614`), folgenlos (Variable ist definiert), für ein künftiges Aufräum-AP vorgemerkt |
| `personal-notes-manager.css` | Feature „Persönliche Notizen". **Seit AP-2.3 (Phase 2, 3.1.100):** 5 Ersetzungen `#fff`/`#ffffff` → `var(--color-background, #fff)`. 15 WP-Admin-Farbschema-Werte (Blau-Verlauf, Grün, Rot, mehrere Grautöne) sowie der `font-family`-Stack von `.cbd-notes-manager-button` (weicht Wort für Wort vom Live-Wert von `--font-family-base` ab) blieben mit Begründungskommentar literal |
| `board-mode.css` | Tafelmodus-Gestaltung (Werkzeugleiste, Zeichenfläche, Dialoge) — größte Einzeldatei des Plugins. **Seit AP-2.6 (Phase 2, 3.1.100):** 38 UI-Chrome-Hex-Werte auf `var(--x, #bisheriger-wert)` umgestellt (Werkzeugleiste, Zeichenflächen-Rahmen, Dialoge). Die Zeichenfarbpalette selbst (Inhalt, von Schülern wählbare Farben, keine UI-Chrome-Farbe) bleibt bewusst unangetastet. `board-mode.js` wurde geprüft, enthielt aber keine UI-Chrome-bezogenen Inline-Styles — unverändert. **Nebenbefund:** `boardPresetHtml` wird erzeugt, aber nie ins DOM eingefügt (toter Code). **Behoben durch AP-1.4 aus `PLAN-PDF-Export-und-Tafelmodus-Fixes.md` (2026-08-25):** der oben genannte Nebenbefund `.cbd-board-confirm-cancel:hover` ist korrigiert (`color` jetzt ebenfalls `var(--color-text-primary, #555)`, Kontrast im Darkmode gemessen ≈7,2:1 statt ≈1,2:1). Zusätzlich behoben: `.cbd-board-header` nutzte `--color-text-primary`/`--color-background` invertiert als Hintergrund/Textfarbe — beide Variablen kehren sich im Darkmode um, die Kopfleiste wäre fälschlich hell geworden; jetzt fest `#333333`/`#ffffff` (bewusst kein Theme-Bezug, die Leiste ist in beiden Modi eine dunkle Fläche). Neuer `[data-theme="dark"]`-Block für `.cbd-board-content`, `.cbd-board-canvas-area`, `.cbd-board-toolbar` (+ `border-top-color`), `.cbd-board-color-picker-overlay`, `.cbd-board-color-picker-label`. **AP-1.5 (dieselbe Änderung, 2026-08-25):** neue Regel `.cbd-board-canvas-container.cbd-board-inverted { filter: invert(1); }` — invertiert Hintergrund-, Gitter- und Zeichen-Canvas gemeinsam (alle drei Ebenen liegen in diesem Wrapper); Klasse wird von `board-mode.js::updateDarkModeInversion()` gesteuert, siehe dort. Live verifiziert: weiße Standardtafel im Darkmode zeigt Notizen invertiert, Grün-/Schwarztafel bleibt unangetastet, mehrfarbige Notiz zeigt erwartete reine Farbinvertierung (Rot→Cyan, Blau→Gelb). **AP-1.fix2 (Korrektur nach AP-1.rev-Befund F1/F3, 2026-08-25):** AP-1.4 hatte die Flächen verdunkelt, aber mehrere Werkzeugleisten-Buttons (Seiten-Navigation, Werkzeugauswahl, Undo, Zoom, Toolbar-Toggle) behielten literale helle Vordergrund-/Hover-Farben aus einer früheren Variablen-Umstellung — Kontrast bis ≈2,2:1 statt WCAG-AA 4,5:1, echte, von AP-1.4 verursachte Regression. Neuer `[data-theme="dark"]`-Block setzt deren Vordergrundfarben auf `var(--color-text-primary, #e8e8e8)`, Hover-Hintergründe auf `var(--color-background-light, ...)` (bewusst **nicht** `var(--color-background)` — das ist die eigene Ruheflächen-Variable dieser Buttons, der Hover wäre sonst unsichtbar geblieben), Aktiv-Zustände auf transparente `rgba(...)`-Tönung der jeweiligen Akzentfarbe. **Befund F10 (dieselbe AP-1.fix2-Korrektur):** `.cbd-board-color-picker-overlay`/`-label` sind aktuell tote Selektoren — `boardPresetHtml` in `board-mode.js` wird zwar gebaut, aber nie ins DOM eingefügt (derselbe tote Code wie `.cbd-board-bg-preset-btn`); die tatsächliche Farbwahl läuft ausschließlich über `.cbd-board-bg-cycle`. Regeln bewusst nicht gelöscht, falls der Dialog künftig verdrahtet wird — jetzt im Datei-Kommentar selbst vermerkt |
| `classroom-frontend.css` | Klassenansicht (`[cbd_classroom]`-Shortcode). **Seit AP-2.2 (Phase 2, 3.1.100):** 23 Ersetzungen (`#fff`/`#ffffff`→`--color-background` 14×, `#e0e0e0`→`--color-sidebar-border` 6×, `#f8f9fa`→`--color-background-light` 2×, `#333`→`--color-text-primary` 1×), alle exakter Live-Wert-Treffer. Material-Design-Statusfarben (Grün/Rot/Blau/Amber) blieben mit Begründungskommentar literal. `classroom-frontend.js` wurde mitgeprüft, enthielt aber keine Hex-Werte in Inline-Styles — unverändert. **Nebenbefund:** mehrere Selektoren (`.cbd-classroom-wrapper`, `.cbd-classroom-form-group`, `#cbd-classroom-auth-form`, `.cbd-classroom-parent-*`, `.cbd-classroom-group`) kommen in keiner PHP/JS-Datei mehr vor (Grep-verifiziert), vermutlich totes CSS aus einer älteren Template-Version — `.cbd-classroom-children` gehörte bis AP-1.2 (siehe unten) ebenfalls dazu und ist seither wieder in Gebrauch; `.cbd-classroom-wrapper` ist in AP-1.1 als tot bestätigt worden (der echte Wrapper heißt `.cbd-classroom-container`, `class-cbd-classroom.php:1125`). **Seit AP-1.1 aus `PLAN-Inhaltsverzeichnisse.md` (2026-08-26):** drei im Darkmode unlesbare Textfarben auf Variablen umgestellt (`.cbd-classroom-wrapper h2` und `.cbd-classroom-page-link` auf `var(--color-text-primary, #1a1a1a)`, `.cbd-classroom-page-grayed` auf `var(--color-text-muted, #9e9e9e)`) — vorher Linktext `rgb(26,26,26)` auf Containerhintergrund `rgb(18,18,18)`. **Seit AP-1.2 (2026-08-26):** neuer Abschnitt am Dateiende, „LOGIN-LISTE: KLAPPBARE BAUMSTRUKTUR (AP-1.2)", rein additiv (`git diff` zeigt keine gelöschte Zeile). Er trägt die Regeln für die neue Baumstruktur aus `classroom-frontend.js`: `.cbd-classroom-page-tree`/`.cbd-classroom-children` (Listenreset, Einzug 24px je Ebene — ersetzt das frühere inline `margin-left`), `.cbd-classroom-page-item:not(.cbd-classroom-expanded) > .cbd-classroom-children { display: none }`, `.cbd-classroom-toggle`/`.cbd-classroom-toggle-icon` samt Drehung um 90° im aufgeklappten Zustand (Muster wie `.page-toggle`/`.toggle-icon` in `Theme/style.css`). **Wichtig für spätere Änderungen:** Das `<li>` ist seit AP-1.2 reine Struktur — es umschließt Zeile UND Kind-Container —, deshalb wandert die Karten-Optik (Rahmen, Polster, Hintergrund, Hover) auf `.cbd-classroom-page-row`; auf dem `<li>` umrahmte die Karte sonst den ganzen Unterbaum, und dessen `:hover` verschöbe beim Überfahren eines Kindes sämtliche Elternknoten mit. Die level-abhängigen Farbbalken `.cbd-classroom-page-item.cbd-level-N` (Spezifität 0-2-0) überleben das `border: none` der neuen `.cbd-classroom-page-item`-Regel (0-1-0) absichtlich und werden dadurch vom Kästchenrand zur senkrechten Ebenenlinie des Unterbaums. Alle neuen Farbwerte stehen als `var(--x, #fallback)`. **Seit AP-1.1 aus `PLAN-Fragenwand.md` (Phase 1, 2026-08-28):** `--cbd-classroom-accent` wird real definiert — `classroom_accent_inline_css()` in `class-cbd-classroom.php` liest `get_theme_mod('color_ui_surface', '#e24614')` und setzt die Property per `wp_add_inline_style()` an allen drei `wp_enqueue_style('cbd-classroom-frontend', ...)`-Stellen. Die sieben Randlinien-Fundstellen (Zeilen 252, 270, 313, 318, 322, 328, 349) sind seither auf `var(--cbd-classroom-accent, #e24614)` statt hartcodierter Grünwerte umgestellt. Zeile 946 (`.cbd-classroom-page-row:hover`) behält bewusst den Fallback `#4caf50` (statt `#e24614`) — er greift nur, falls das Inline-Style einmal fehlt. Der Kommentar bei Zeile 874-881 behauptet weiterhin fälschlich, die Variable sei „nirgends definiert" — bekannte, noch offene Dokumentationslücke, siehe `CLAUDE.md`, Abschnitt „Klassenmodus-Anmeldebaum: Theme-Akzentfarbe statt Grün" |
| `fragenwand.css` | **Frontend-Gestaltung des Fragenwand-Modals** (Vorhaben „Fragenwand" AP-3.4, neu) — Post-it-Optik der Notizen (`.cbd-fragenwand-notiz`: Hintergrund `var(--color-background-light, #f8f9fa)`, Rahmen `var(--color-border, #dddddd)`, `rgba(0,0,0,0.15)`-Schlagschatten), Overlay/Dialog (`.cbd-fragenwand-overlay`/`-dialog`), Klassenauswahl-Dialog (`.cbd-fragenwand-klassenwahl-overlay`/`-dialog`, Buttons `var(--color-ui-surface, #e24614)`/Hover `var(--color-ui-surface-dark, #c93d12)`). Erledigte Notizen sichtbar, aber gedämpft: `.cbd-fragenwand-notiz--erledigt` = `opacity: 0.55` + `text-decoration: line-through` + `color: var(--color-text-muted, #666666)`, **kein** `display: none`. Ausschließlich `var(--x, #fallback)` bzw. `rgba(0,0,0,…)`-Schatten, keine freistehenden Hex-Werte (63 Hex-Vorkommen geprüft, alle als Fallback). Automatische Darkmode-Fähigkeit ohne eigenen `[data-theme="dark"]`-Block, da alle Farben über Theme-Variablen laufen. **Im selben AP gefundene und behobene CSS-Falle:** `[hidden]` auf `.cbd-fragenwand-notiz__label`/`__aktionen` wurde von der gleich spezifischen `display:flex`-Klassenregel überstimmt (Label/Aktionsknöpfe blieben beim Bearbeiten fälschlich sichtbar) — Fix über explizite `[hidden] { display: none; }`-Regeln. Eingereiht von `CBD_Fragenwand::enqueue_frontend_assets()` (Handle `cbd-fragenwand`), **unbedingt auf jeder Frontend-Seite**, unabhängig von `has_block()` — der Trigger kann auch im Theme-Inhaltsverzeichnis (Phase 4) außerhalb von `post_content` erscheinen. **Seit AP-4.fix1 zusätzlicher Abschnitt „INHALTSVERZEICHNIS-EINTRAG"** (Klassennamen kommen aus `page_index_eintrag()` in `class-cbd-fragenwand.php`, nicht aus `fragenwand-frontend.js` wie der Rest dieser Datei): `.page-index__zusatz--fragenwand` (Abstand nach unten) und `.page-index__fragenwand-link` (Hintergrund `var(--color-ui-surface, #e24614)`, Text `var(--color-text-on-accent, #ffffff)`, `cursor: pointer`, abgerundete Ecken, Hover `var(--color-ui-surface-dark, #c93d12)`, `:focus-visible`-Kontur) — behebt den AP-4.rev-Befund „grauer Browser-Standard-Knopf ohne eigenes CSS". **Vorhaben „Schüler-Fragen" (3.1.113):** `.cbd-fragenwand-notiz--schueler` (4px linker Rahmen) und `.cbd-fragenwand-notiz__herkunft` (kleine Beschriftung „Schülerfrage") — beides in `--cbd-fragenwand-schueler`, dem **einzigen eigenen Farbwert dieser Datei** (#5b6bbf hell / #9aa8e8 dunkel) samt dem **einzigen eigenen `[data-theme="dark"]`-Block**: Das Projekt hat keine passende Variable, und die Akzentfarbe ist über den Customizer frei einstellbar. Beide Zustände wirken gleichzeitig mit `--erledigt` (kein Selektor fasst dieselbe Eigenschaft zweimal an). Dazu `.cbd-fragenwand-neu--schueler`/`__zeile`/`__hinweis` für das Schüler-Eingabefeld |
| `fragenwand-editor.css` | **Winzige Editor-Kennzeichnung des Inline-Verweises** (AP-3.4, neu) — `a.cbd-fragenwand-verweis` bekommt im Editor eine gepunktete Unterstreichung in `var(--color-ui-surface, #e24614)`, weil der Editor sonst keine eigene Verlinkungsfarbe für diese Klasse zeigt und ein eingefügter Verweis nicht von normalem Text zu unterscheiden wäre. Bewusst eine **eigene, separate** Datei statt `fragenwand.css`: Das Frontend-CSS enthält modalspezifische Regeln (Overlay, Post-it-Karten, Klassenauswahl), die im Editor nur totes Gewicht wären. Lädt **ausschließlich** im Editor, über zwei Wege gleichzeitig (`CBD_Fragenwand::register_editor_format()`, siehe dortige Zeile in „Kernklassen" oben): ein gewöhnliches `wp_enqueue_style()` auf `enqueue_block_editor_assets` **und** `inject_editor_style_into_iframe()` am Filter `block_editor_settings_all` — der Block-Editor rendert seit WordPress 5.9 in einem eigenen `<iframe>` (`editor-canvas`), das der erste Weg allein nicht erreicht; ohne den zweiten Weg blieb der Verweis im Editor optisch nicht vom WordPress-Standardlink unterscheidbar (in AP-3.4 gefunden und behoben) |
| `lehrer-klassen.css` | **Klassenverwaltung im Frontend** (`[cbd_lehrer_klassen]`, seit 3.1.115) — eingereiht von `CBD_Classroom::enqueue_lehrer_klassen_assets()`, sobald der Shortcode auf der Seite steht (auch für den Hinweis- und den Anmeldekasten, nicht erst für die Oberfläche). **Ist NICHT bloß eine Frontend-Kopie von `classroom-admin.css`:** Die Oberfläche benutzt unverändert `classroom-admin.js` und damit dessen WP-Admin-Klassennamen (`button`, `button-primary`, `button-small`, `button-link-delete`, `notice`, `notice-success/-error/-info`, `wp-list-table`, `form-table`, `regular-text`, `description`, `spinner is-active`); im Frontend gibt es dafür keine Regeln, diese Datei liefert sie nach. Alles unter `.cbd-lehrer-klassen` gescopt, ausschließlich `var(--x, #fallback)`, Dunkelmodus über `[data-theme="dark"]`. Eigene Variablen nur für Zustandsfarben (`--cbd-lk-ok-*`, `--cbd-lk-warn-*`, `--cbd-lk-abo-*`, `--cbd-lk-eigen-bg`, `--cbd-lk-loeschen`) — dieselbe bewusste Ausnahme wie `--cbd-fragenwand-schueler` in `fragenwand.css`. **Drei live nachgebesserte Stellen, die nicht wegoptimiert werden dürfen:** `min-width: 0` auf `.cbd-page-selector select` (ohne das schrumpft ein `<select>` nie unter seine längste Option und schneidet den Entfernen-Knopf ab), `max-height` + eigener Bildlauf auf `.cbd-page-tags` (eine Klasse kann siebzig Seiten tragen), und die Klassenliste bleibt auf schmalen Anzeigen eine **echte Tabelle** mit `overflow-x: auto` am umgebenden Abschnitt statt gestapelter `display: block`-Zellen — gemessen ergab das Stapeln Zeilenhöhen um 600 px, weil das Theme eigene Zellregeln mitbringt. Details: `CLAUDE.md`, Abschnitt „Klassenverwaltung im Frontend" |
| `icon-picker.css`, `classroom-admin.css`, `admin*.css`, `edit-block-form.css`, `new-block-form.css` | jeweils zum gleichnamigen Bereich |

## Werkzeuge und Tests (`tools/`)

**Wird bewusst NICHT ins Verteilungs-ZIP aufgenommen** (`create-plugin-zip.js`
listet nur `admin`, `assets`, `blocks`, `includes`, `vendor`, `languages`).

| Datei | Zweck |
|---|---|
| `check-php74.php` | Prüft alle Plugin-Dateien gegen PHP 7.4. **Zwingend**, weil lokal PHP 8.x läuft und `php -l` 8.0-Syntax nicht meldet, die Zielumgebung aber 7.4.33 ist. Läuft automatisch im ZIP-Bau |
| `test-block-serializer.php` | 71 Prüfungen in vier Gruppen: Fragmentebene, Dokumentebene, Markup-Treue gegen die Fixture, Delimiter-Bilanz |
| `test-design-transfer.php` | Beide Transferformate, Rundläufe, heikle Werte |
| `test-icon-library.php`, `test-icon-manager.php`, `test-icon-value.php`, `test-icon-scale.php` | Icon-Bestand, Dateinamen, Speicherformat, Größenskalierung |
| `test-svg-sanitizer.php` | 36 Prüfungen: Angriffsmuster raus, legitime Kacheln heil |
| `test-page-importer.php` | Elternseite im Seitenimport (Vorhaben „Importer-Elternseite", nach TDD entstanden: roter Commit `f51e424` vor grünem `e4207bf`). **34 Prüfungen** ohne WordPress: `bereinige_elternseite()` (AK1–AK5: ungültige Werte → `0`, gültige ID unverändert, keine PHP-Warnung bei überlanger Ziffernfolge, Slashes wie aus `$_POST`) und ihre Verdrahtung in `ajax_seiten_importieren()` (`post_parent`, erhaltene `wp_slash()`-Maskierung) |
| `test-page-importer-kaskade.js` | Vorhaben „Seitenimporter-Kaskaden-Zielauswahl" (AP-1.fix1, nachträglich versioniert — der in der AP-1.2-Übergabenotiz beschriebene Harnisch existierte zunächst nicht im Repo, Review-Befund B2). **Läuft mit `node`, ohne jsdom**, nach demselben Muster wie `test-block-auswahl.js`: `page-importer.js` exportiert selbst nichts nach `window`, der Harnisch liest die Datei als Text, prüft, dass die Ankerzeile `var KONF = window.cbdPageImport;` genau einmal vorkommt, und fügt dort eine einzige Zeile ein, die die (dank Function-Hoisting bereits existierenden) Kaskadenfunktionen auf `window.__cbdTestHooks__` legt — kein Funktionsrumpf wird verändert. Läuft über `new Function('window', 'document', quelle)` gegen einen selbstgeschriebenen, minimalen DOM-Stub. **64 Prüfungen:** Hausstil (kein `.innerHTML =`), Ebene-1-Aufbau aus `wurzeln`, Drill-down bei Kindern, kein Anhängen bei einem Blatt, Pruning bei erneuter Wahl in einer höheren Ebene, Rücksprung auf „oberste Ebene", der B1-Fall (Rücksprung auf „diese Seite als Elternseite" in einer Zwischenebene hängt keine Doppel-Ebene mehr an), `kaskadeFehler()`, `kaskadeSperren()`, alle drei `kaskadeLaden()`-Pfade inkl. Verifikation des angefragten Pfads `/cbd/v1/seitenbaum?entwuerfe=1`. **Testfang selbst geprüft:** Der Fix wurde per `git stash` temporär entfernt — dabei schlug exakt die neue B1-Prüfung fehl, alle anderen 63 blieben grün |
| `fixtures/referenz-markup.html` | **Gemessenes Zielmarkup** aus einer echten Editor-Speicherung — Vorlage für den Serializer |
| `fixtures/referenz-umgebung.md` | Herkunft, Versionen und was daraus abgelesen wurde |
| `test-classroom-gate.php` | Klassen-Durchlass: Zerlegung der `container_id`, behandelte Container je Seite und Klasse. Eine Prüfung wacht darüber, dass die Suffix-Regel `:pN` nur **einmal** im Code steht |
| `test-klassenpuls.php` | Klassenpuls-Route `cbd/v1/klassenpuls` (Phase 1 von `PLAN-Klassenmodus-Live.md`, AP-1.2, nach TDD entstanden: roter Commit `6f8006d` — Implementierung folgt in AP-1.3, danach grün). **22 Prüfungen** in vier Gruppen: A `baue_signatur()` rein ohne DB (5 — Stabilität, Kollisionsfreiheit, 12-stelliges Hex-Format, `null`/`''` gleichwertig, Reihenfolge zählt), B `takt()` (7 — Vorgabe 10, String-Zahlen, `0` = abgeschaltet, Klemmung auf 5…300, ungültige Werte → Vorgabe, negative Werte → abgeschaltet), C Konstanten und `register_routes()` (4 — `REST_NAMESPACE`/`REST_ROUTE`/`FEHLERSTATUS`, genau eine GET-Route mit `permission_callback` `__return_true`), D textbasierte Wächter gegen eine zweite Token-Deutung (6 — kein `get_transient`, keine Zeichenfolge `cbd_classroom_`, verwendet `CBD_Classroom_Gate::sitzung`, ruft `nocache_headers`, kein `GROUP_CONCAT`, nutzt `$wpdb->prepare`). Läuft komplett ohne WordPress; Aufrufe auf statische Methoden/Konstanten laufen über `ruf_statisch()`/`konstante()` (Reflection), damit eine noch fehlende Klasse `CBD_Klassenpuls` einen sauberen Fehlschlag statt eines Fatal Error ergibt — Gruppe-D-Prüfungen setzen zusätzlich `file_exists()` voraus und sind bei fehlender Datei zwangsläufig alle rot. Vorbild für Aufbau und Stil: `test-classroom-gate.php` |
| `test-icon-position.php` | Datenmodell und Sanitizer der Icon-Position (AP-2.1, nach TDD entstanden: roter Commit `950efb5` vor grünem `4f0002f`). 57 Prüfungen: fünf Positionen und Grenzen ±200, **Altwerte → `header`**, Groß-/Kleinschreibung, Markup-Eingaben, deutsches Dezimalkomma (`12,5` → `13`), Klemmung, Klassen- und Style-Erzeugung ohne Dezimalkomma, Rundlauf mit Slashes wie aus `$_POST`, Integration in `cbd_parse_features_from_post()` |
| `test-inline-reference.php` | Vertrag D und E der Serverseite des Inline-Verweises (AP-3.3, nach TDD entstanden: roter Commit `d2e0597` vor grünem `0df80cd`; erweitert in AP-3.fix2, AP-3.fix5, AP-4.2, AP-4.fix1, AP-4.fix2). **181 Prüfungen** (177 im Doppel-Modus, weiterhin 4 sichtbare Skips), **zweimal gefahren**: gegen die echte `WP_HTML_Tag_Processor` (aus einer gefundenen WordPress-Installation geborgt, ohne WordPress zu booten) und gegen ein schmales Doppel (`CBD_TEST_TAG_PROCESSOR=doppel`). Schwerpunkt ist die **Zeichengleichheit**: Prüfungen belegen mit `===`, dass Inhalt ohne Inline-Verweis unangetastet bleibt — darunter `<script>` mit `if (a < b)`, eine JS-Regex, die vollständige `render.php`-Ausgabe des Blocks, Container-Markup mit `data-stable-id`, Umlaute und LaTeX. Dazu Fälle unbrauchbarer `data-target-post`, die href-Bildung mit und ohne Anker und mit vorhandener Abfrage, `get_permalink()` leer, Filterpriorität gegen die im Repo belegten 11 und 10000, und dass ohne `format.js` **nichts** registriert und **keine** Warnung ausgegeben wird. **Eine Prüfung fing einen Hoisting-Fehler im Harnisch selbst:** das unbedingt deklarierte Klassendoppel machte den Nachweis „fehlender Vertragspartner erzeugt keinen Fatal Error" wertlos — es steht jetzt in einem `eval()`. **Aus AP-3.fix2:** überlange Ziffernfolgen (20, 30, 100 Stellen) müssen zeichengleich bleiben **und dürfen keine PHP-Warnung erzeugen** (eigener `set_error_handler` macht jede Warnung zum Fehlschlag), `PHP_INT_MAX` bleibt eine gültige ID, plus elf Struktur-Randfälle, von denen vier das Doppel nicht tragen kann (keine Rohtext-Elemente, keine Kommentare) und dort mit sichtbarer `SKIP`-Zeile samt Begründung übersprungen werden. **Aus AP-3.fix5:** führende Nullen (`045`, `00…045`) bleiben zeichengleich, dokumentiert statt „repariert". **Aus AP-4.2/AP-4.fix1:** Duplikatswächter für die Registrierungsgruppe (Gruppe 3c, geprüft über den Test-Seam `format_script_daten()`, ohne `format.js` je anzulegen) und die `wp-data`-Abhängigkeit. **Aus AP-4.fix2 (Gruppe 11):** Duplikatswächter für die Klassenzeichenkette an **vier** Stellen (`class-cbd-inline-reference.php`, `format.js`, `view.js`, `style.css`), geprüft am **wirksamen Ausdruck** statt am bloßen Vorkommen — das Anschlagen des Wächters ist per Mutation im Speicher selbst mitgeprüft (Quelltext-Zusicherung, keine Repository-Änderung) |
| `test-block-auswahl.js` | Der gemeinsame Auswahlbaustein `window.cbdBlockAuswahl` (AP-3.2, nach TDD entstanden: roter Commit `5d34cf2` vor grünem `5fb4e8c`; erweitert in AP-3.fix4). **Läuft mit `node`, ohne jsdom und ohne npm-Abhängigkeit** — die Quelldatei wird über `new Function('window', quelle)` mit einem Stub-`window` ausgeführt. **140 Prüfungen**: die sieben öffentlichen Namen und **kein achter**, Memoisierung (drei gleichzeitige Aufrufe = ein Promise, zwei `apiFetch`-Aufrufe), vier Netzfehler-Fälle ohne Reject, Kaskade mit gleichzeitiger Unterseiten- und Blockstufe, Beschneidung blockloser Zweige, Beiträge über die Wurzelmarke, `pfadVon()` mit unbekannter ID und mit Zyklus, Quellprüfung auf `import`/`export`/`class`/`console.log`. **Aus AP-3.fix4:** Ein gelöschter, aber noch als `wert` gesetzter Zielblock darf in der Blockstufe keine Option mehr anbieten, die beim Anklicken `melde(null)` auslöst — nur das ausdrückliche Abwählen darf das. **`wp` wird als nicht aufzählbarer, protokollierender Getter gestubbt** — so ist beweisbar, dass die Datei es beim Laden nicht anfasst. **Nicht geprüft:** die Komponente `HierarchieAuswahl` selbst (ohne React-Umgebung nicht sinnvoll), nur ihr Wachposten; ihre Abnahme erfolgt an der Oberfläche |
| `test-seitenbaum.php` | Vertrag A und B der beiden Redakteurs-Routen (AP-3.1, nach TDD entstanden: roter Commit `85e1bc9` vor grünem `3a50704`; erweitert in AP-3.fix1, rot `1194e2b` vor grün `e933ee0`; erneut in AP-3.fix3, rot `bf0bbfc` vor grün `db58678`). **97 Prüfungen:** die drei neuen Felder in `cbd/v1/blocks`, Baumaufbau über vier Ebenen, Sortierung `menuOrder` vor `titel`, verwaister Knoten fällt samt Unterbaum heraus, Zyklus A→B→A von der Wurzel unerreichbar, Beiträge nicht im Baum, Memoisierung innerhalb einer Anfrage. **Zur `gesperrt`-Ermittlung (AP-3.fix1):** alle drei Stufen der Kette einzeln, mit Zählern auf **beiden** Theme-Funktionen — Stufe 1 genau einmal aufgerufen und Stufe 2 gar nicht, Abfragenzahl konstant bei 5 und bei 50 Seiten mit mindestens einer gesperrten Seite, und **eine Unterseite ohne eigene Meta erbt `gesperrt: true`**. Letzterer Fall fehlte in AP-3.1 vollständig — deshalb war dort die falsche Behauptung entstanden, die Theme-Funktion vererbe die Sperre nicht. **Seit „Seitenimporter-Kaskaden-Zielauswahl" (2026-08-25, AP-1.1) auf 103 Prüfungen erweitert** (97 Alt- + 6 neue): der optionale Parameter `entwuerfe` erweitert die `WHERE`-Klausel auf `post_status IN ('publish', 'draft')` bei `entwuerfe=1`, bleibt sonst byte-identisch beim bisherigen `post_status = 'publish'`, und die Cache-Isolation zwischen den beiden Parametervarianten (`self::$seitenbaum_cache['ohne_entwuerfe']`/`['mit_entwuerfe']`) innerhalb derselben Anfrage liefert nachweislich nicht vermischte Ergebnisse. **Aus AP-3.fix3 dazugekommen:** `json_encode()` der Antwort muss für `knoten` **und** `kinder` in allen vier Baumformen ein **Objekt** liefern (flach, hierarchisch, einzelne Wurzel, leer) — ein PHP-Array mit den Schlüsseln `0..n-1` würde zur JSON-Liste, die der Client stillschweigend verwirft; `update_meta_cache()` wird bei vorhandener Stufe 1 **gar nicht** und bei Stufe 2 **genau einmal** gerufen, letzteres an einer Stelle geprüft, an der nur Stufe 2 definiert ist; und `orderby` ist per Quelltext-Zusicherung auf `title` festgenagelt (Regressionsschutz, damit es kein drittes Mal umgestellt wird) **Quelltext-Zusicherung:** Prüfgruppe 3 liest die SQL-Zeichenkette selbst und verlangt fünf einzeln benannte Spalten, kein `SELECT *`, kein `post_content` |
| `test-block-content-api.php` | Autorisierung und Blocksuche des Endpunkts `cbd/v1/block-html` (AP-2.4). 84 Prüfungen: Fund über Attribut und über den `data-stable-id`-Rückfall, Rekursion in `innerBlocks`, unbekannte Kennung → Ablehnung, fremder Namensraum → Ablehnung, Theme-Funktion liefert `false` → Ablehnung, Theme-Funktion **nicht definiert** → kein Fatal, **Ablehnung und Nichtexistenz zeichengleich**, keine Seitenmetadaten in der Antwort, Ausnahme beim Rendern → 404, globaler `$post` danach aufgeräumt |
| `test-latex-parser.php` | Prüfharnisch LaTeX-Parser (seit 3.1.88), **113 Prüfungen**: Filterprioritäten, alle fünf Delimiter, `<span>` statt `<div>`, Doppelparse-Schutz, Lade-Gate `should_load_katex()`, die Folgen der `the_content`-Priorität 11 (klassische Inhalte nach `wpautop`/`wptexturize`) und seit AP-1.fix2: Code-Blöcke bleiben zeichengleich (Blocknamen-Filter **plus** Maskierung von `script`/`pre`/`code`, `blockName null` ausgenommen) und HTML-Entities erreichen KaTeX aufgelöst |

## Dokumentation (`docs/`)

| Datei | Zweck |
|---|---|
| `VERBESSERUNGSPLAN.md` … `-5.md` | Review-Runden 2026-07, Arbeitspakete mit Status |
| `AENDERUNGEN-UND-UPLOAD.md` | Gesamtübersicht plus Upload- und Recovery-Anleitung |
| `PLAN-Seitenimport.md` | Projektplan „Seitenimport + Sammelaktionen" mit Statustabelle und Testprotokoll |
| `ERWEITERUNGSANALYSE-Seitenimport.md` | Analyse, die zum Plan führte (Abschnitt 3.2 enthält eine Korrektur) |
| `ERWEITERUNGSANALYSE-Klassenmodus-Live.md` | Analyse-Vorlauf zu `PLAN-Klassenmodus-Live.md` (Website-Ordner, Stand 2026-08-30): Ist-Zustand der drei Schüleransichten, die verworfenen Push-Alternativen (SSE, Heartbeat) und die Begründung für die zweistufige Abfrage. Details der Umsetzung: `CLAUDE.md`, Abschnitt „Klassenmodus: Live-Aktualisierung" |
| `messung-klassenpuls.md` | Lastmessbericht `cbd/v1/klassenpuls` (AP-1.7, `PLAN-Klassenmodus-Live.md`, Website-Ordner): Einzel-/Vergleichsmessung gegen eine gewöhnliche Seite, Sättigungskurve über fünf Parallelitätsstufen (gültige und ungültige Sitzung, Knick bei 25 Gleichzeitigen), Gleichschritt-Prüfung über eine Nachbildung des `klassenpuls.js`-Ablaufplans (echte Browser-Tabs in der verfügbaren Automatisierung nicht durchführbar, da `document.hidden` dort dauerhaft `true` bleibt — Details im Bericht) |
| `pruefung-produktiv.js` | Konsolenskript: liest WordPress-Version, Tabellenspalten und Designs der Produktivinstallation |
| `pruefung-blockmarkup.js` | Konsolenskript: liest das Blockmarkup einer Seite über `getEditedPostContent()` — für das Neuerheben der Fixture nach einem Update |
| `archiv/` | historische Status-Notizen, nicht gepflegt |

## Sammelzeilen

| Pfad | Anmerkung |
|---|---|
| `vendor/` | Composer-Abhängigkeiten (mPDF, TCPDF). **Versioniert und im ZIP enthalten** — nicht mit `.gitignore` ausschließen |
| `languages/` | Übersetzungsdateien |
| `assets/icons/` | Ausgelieferte SVG-Kacheln (`kategorien/`, `zahlen/`, `ui/`). Bestand steht nicht im Code, `CBD_Icon_Library` scannt zur Laufzeit |
| `dist/` | Gebaute ZIPs, nicht versioniert |
| `node_modules/` | nur für die Bauwerkzeuge, nicht versioniert |
